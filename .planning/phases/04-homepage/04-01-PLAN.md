---
phase: 04-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/homepage/HomepageHero.tsx
  - src/components/homepage/TrustBar.tsx
  - src/components/homepage/WhatToExpect.tsx
  - src/components/homepage/FirstTimerSpotlight.tsx
autonomous: true

must_haves:
  truths:
    - "Hero section fills the viewport edge-to-edge under the transparent header with no cream gap"
    - "Hero displays a comfort-first headline, subheadline, Book Now CTA, and First-Timer Guide CTA"
    - "Clicking First-Timer Guide smooth-scrolls to What to Expect section without header clipping"
    - "Trust bar shows 4 stats (Google rating, years, client count, certification) immediately after hero"
    - "What to Expect shows 3-5 icon cards addressing first-visit anxieties"
    - "First-Timer Spotlight displays package name, $70 price, Save $17, visual checklist, and dedicated CTA"
  artifacts:
    - path: "src/components/homepage/HomepageHero.tsx"
      provides: "Full-viewport hero with gradient placeholder, trust badge, two CTAs"
      contains: "-mt-16"
    - path: "src/components/homepage/TrustBar.tsx"
      provides: "4-stat horizontal trust bar"
      contains: "clientConfig"
    - path: "src/components/homepage/WhatToExpect.tsx"
      provides: "Icon card grid for first-visit steps"
      contains: "what-to-expect"
    - path: "src/components/homepage/FirstTimerSpotlight.tsx"
      provides: "First-Timer Package highlight with pricing and CTA"
      contains: "PACKAGE_SLUGS"
  key_links:
    - from: "src/components/homepage/TrustBar.tsx"
      to: "src/content/client.config.ts"
      via: "clientConfig import"
      pattern: "clientConfig\\.(reviewAverage|reviewCount|yearsInBusiness)"
    - from: "src/components/homepage/FirstTimerSpotlight.tsx"
      to: "src/content/services/index.ts"
      via: "getServiceBySlug lookup"
      pattern: "getServiceBySlug\\(PACKAGE_SLUGS\\.firstTimer\\)"
    - from: "src/components/homepage/HomepageHero.tsx"
      to: "src/components/homepage/WhatToExpect.tsx"
      via: "anchor href scroll"
      pattern: 'href="#what-to-expect"'
---

<objective>
Build the top-of-page homepage sections: HomepageHero, TrustBar, WhatToExpect, and FirstTimerSpotlight. These four sections form the emotional conversion funnel — a first-time visitor sees the hero, gets immediate trust signals, learns what to expect, and sees the First-Timer Package — all before scrolling past the fold on desktop.

Purpose: The top half of the homepage is the first-timer anxiety reducer. Every section above ServicesPreview exists to move a nervous first-timer from "I'm curious" to "this feels safe."
Output: 4 Server Components (HomepageHero may be "use client" if parallax is used) in src/components/homepage/.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-homepage/04-RESEARCH.md
@.planning/phases/04-homepage/04-CONTEXT.md
@src/components/ui/SectionWrapper.tsx
@src/components/ui/index.ts
@src/components/ui/BookingLink.tsx
@src/components/ui/Button.tsx
@src/components/ui/Badge.tsx
@src/components/ui/FadeUp.tsx
@src/components/ui/StarRating.tsx
@src/content/client.config.ts
@src/content/services/index.ts
@src/content/services/packages.ts
@src/content/staff.ts
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: HomepageHero + TrustBar</name>
  <files>src/components/homepage/HomepageHero.tsx, src/components/homepage/TrustBar.tsx</files>
  <action>
  Create the `src/components/homepage/` directory.

  **HomepageHero.tsx:**
  - Full-viewport hero section (`min-h-screen`) with CSS gradient placeholder background (dark forest green to sage: `from-[#2C3E2D] via-brand-dark to-brand-secondary/60`). Add a comment: `{/* Replace with: <Image fill src="/images/hero.jpg" className="object-cover" alt="" priority /> */}`
  - Apply `-mt-16 lg:-mt-20` to negate the main padding from layout.tsx so the hero fills edge-to-edge under the transparent header. Add `pt-32 lg:pt-40` on the inner content container to push text below the header.
  - Headline (Fraunces heading font): "Your First Wax Should Feel Good" — addresses first-timer anxiety directly.
  - Subheadline: "Honey-based wax. A patient, judgment-free esthetician. And a First-Timer Package designed to make your first visit the best one."
  - Primary CTA: `<BookingLink variant="cta">Book Now</BookingLink>`
  - Secondary CTA: `<Button variant="outline" href="#what-to-expect">First-Timer Guide</Button>` — plain anchor, smooth scroll handled by CSS `scroll-behavior: smooth` in globals.css. Style the outline button for dark background (white text/border).
  - Trust badge near CTAs: `<Badge variant="trust">` showing star rating and review count from clientConfig (e.g., "4.9 — 247+ happy clients").
  - All text white (`text-white`) since the background is dark.
  - Do NOT add "use client" unless using scroll hooks. If static gradient background, keep as Server Component. FadeUp can wrap content without escalating.
  - Wrap headline and CTA group in FadeUp for entrance animation.

  **TrustBar.tsx:**
  - Server Component (no "use client").
  - `<SectionWrapper bg="dark" padding="sm">` — charcoal background immediately after hero for maximum trust signal contrast.
  - 4 stats in a horizontal grid: `grid grid-cols-2 lg:grid-cols-4 gap-6 text-center`.
  - Stats pulled from clientConfig: (1) Google rating with star, (2) years in business, (3) review count + "Happy Clients", (4) "Licensed Esthetician" from staff certifications or a static "Licensed & Certified" label.
  - Use Fraunces (`font-heading`) for stat values, muted white (`text-white/70`) for stat labels.
  - Wrap in FadeUp for entrance animation.
  </action>
  <verify>
  Run `npx tsc --noEmit` — both files compile with zero errors. Visually confirm: hero has no cream gap at top, trust bar renders 4 stats.
  </verify>
  <done>
  HomepageHero fills the viewport edge-to-edge with gradient background, displays comfort-first headline, two CTAs (Book Now links to bookingUrl, First-Timer Guide links to #what-to-expect), and a trust badge. TrustBar shows 4 stats from clientConfig on a dark background.
  </done>
</task>

<task type="auto">
  <name>Task 2: WhatToExpect + FirstTimerSpotlight</name>
  <files>src/components/homepage/WhatToExpect.tsx, src/components/homepage/FirstTimerSpotlight.tsx</files>
  <action>
  **WhatToExpect.tsx:**
  - Server Component (no "use client").
  - `<SectionWrapper id="what-to-expect" bg="light" padding="lg" className="scroll-mt-20">` — the `id` is the anchor target for the hero CTA, `scroll-mt-20` prevents the fixed header from clipping the heading.
  - Section heading: "What to Expect on Your First Visit" (H2, Fraunces font).
  - Brief intro paragraph addressing anxiety: something like "We know the first time can feel nerve-wracking. Here's exactly what happens — no surprises."
  - 4 icon cards in a grid (`grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8`):
    1. **Before You Arrive** — prep info (hair growth, loose clothing, skip lotion). Icon: inline SVG calendar or checklist.
    2. **When You Walk In** — warm greeting, private room, walkthrough. Icon: inline SVG door/welcome.
    3. **During Your Service** — honey-based wax, quick application, talked through each step. Icon: inline SVG heart/hand.
    4. **After You Leave** — aftercare kit, what to expect 24-48hrs, when to rebook. Icon: inline SVG sparkle/star.
  - Each card: centered icon (48x48, `text-brand-primary`), H3 title, short description paragraph.
  - Wrap each card in `<FadeUp delay={i * 0.1}>` for staggered entrance.
  - Write 4 inline SVGs directly in the component — do NOT install an icon library. Keep SVGs simple (24x24 viewBox, stroke or fill, ~3-5 path elements max).

  **FirstTimerSpotlight.tsx:**
  - Server Component (no "use client").
  - `<SectionWrapper bg="blush" padding="lg">` — softer cream variant for warmth.
  - Data: `const pkg = getServiceBySlug(PACKAGE_SLUGS.firstTimer)!` — non-null assertion is safe since the package is guaranteed in the data layer.
  - Section heading: "The First-Timer Package" (H2, Fraunces).
  - Price display: `$70` large and bold, with "Save $17" as a badge or callout next to it. Pull price from `pkg.price`. The $17 savings is a known marketing figure — display as text "Save $17 vs. a-la-carte".
  - Visual checklist: three items with checkmarks — "Brazilian Wax", "Eyebrow Shaping", "Aftercare Kit". Use a green checkmark (text-brand-secondary) before each item. These are known from the package data — use static strings since they're display marketing copy derived from the package contents.
  - Short description from `pkg.shortDescription`.
  - Dedicated CTA: `<BookingLink variant="cta">Book the First-Timer Package</BookingLink>`
  - Layout: centered on mobile, consider a two-column layout on desktop (checklist left, CTA + description right) or a single centered card — Claude's discretion for visual balance.
  - Wrap in FadeUp for entrance animation.
  </action>
  <verify>
  Run `npx tsc --noEmit` — both files compile with zero errors. Click "First-Timer Guide" in hero — page smooth-scrolls to What to Expect heading visible below the fixed header. FirstTimerSpotlight shows the correct price from the data layer.
  </verify>
  <done>
  WhatToExpect renders 4 icon cards addressing first-visit anxieties with id="what-to-expect" and scroll-mt-20. FirstTimerSpotlight displays the First-Timer Package with price ($70), savings ($17), visual checklist, description from data layer, and a dedicated Book Now CTA.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- All 4 component files exist in src/components/homepage/
- No "framer-motion" imports — only "motion/react" if motion is used
- clientConfig values used (not hardcoded numbers) in TrustBar
- getServiceBySlug used (not hardcoded price) in FirstTimerSpotlight
- Hero has -mt-16 lg:-mt-20 to negate main padding
- WhatToExpect has id="what-to-expect" and className="scroll-mt-20"
</verification>

<success_criteria>
- 4 homepage section components compile and render with real data from the content layer
- Hero fills viewport edge-to-edge with no cream gap under transparent header
- Smooth scroll from hero CTA to What to Expect works without header clipping
- First-Timer Spotlight shows dynamic pricing from the service data layer
</success_criteria>

<output>
After completion, create `.planning/phases/04-homepage/04-01-SUMMARY.md`
</output>
