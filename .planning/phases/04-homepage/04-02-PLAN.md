---
phase: 04-homepage
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/homepage/ServicesPreview.tsx
  - src/components/homepage/TestimonialsGrid.tsx
  - src/components/homepage/EstheticianIntro.tsx
  - src/components/homepage/FinalCTA.tsx
  - src/components/homepage/index.ts
  - src/app/page.tsx
autonomous: true

must_haves:
  truths:
    - "Services preview shows 4 category cards (Face, Body, Intimate, Packages) with icon, name, tagline, and View Services link"
    - "Testimonials section displays exactly 3 review cards with star rating, quote, author name, and service"
    - "Esthetician intro shows Maya Chen's photo placeholder, name, title, and bio from the staff data"
    - "Final CTA section shows a warm booking prompt with Book Now button and trust reinforcement"
    - "Homepage at / renders all 8 sections in the locked order: Hero, TrustBar, WhatToExpect, FirstTimerSpotlight, ServicesPreview, TestimonialsGrid, EstheticianIntro, FinalCTA"
    - "The page compiles and renders at localhost:3000 with no TypeScript errors"
  artifacts:
    - path: "src/components/homepage/ServicesPreview.tsx"
      provides: "4-category service card grid with links to /services/[slug]"
      contains: "serviceCategories"
    - path: "src/components/homepage/TestimonialsGrid.tsx"
      provides: "Static 3-card testimonial grid"
      contains: "testimonials.slice"
    - path: "src/components/homepage/EstheticianIntro.tsx"
      provides: "Photo + text split layout for lead esthetician"
      contains: "staff"
    - path: "src/components/homepage/FinalCTA.tsx"
      provides: "Warm closing CTA with booking link"
      contains: "BookingLink"
    - path: "src/components/homepage/index.ts"
      provides: "Barrel export for all 8 homepage sections"
      exports: ["HomepageHero", "TrustBar", "WhatToExpect", "FirstTimerSpotlight", "ServicesPreview", "TestimonialsGrid", "EstheticianIntro", "FinalCTA"]
    - path: "src/app/page.tsx"
      provides: "Thin composer importing all sections with generatePageMetadata"
      contains: "generatePageMetadata"
  key_links:
    - from: "src/components/homepage/ServicesPreview.tsx"
      to: "src/content/services/categories.ts"
      via: "serviceCategories import"
      pattern: "serviceCategories"
    - from: "src/components/homepage/TestimonialsGrid.tsx"
      to: "src/content/testimonials.ts"
      via: "testimonials import and slice"
      pattern: "testimonials\\.slice\\(0,\\s*3\\)"
    - from: "src/components/homepage/EstheticianIntro.tsx"
      to: "src/content/staff.ts"
      via: "staff[0] import"
      pattern: "staff\\[0\\]"
    - from: "src/app/page.tsx"
      to: "src/components/homepage/index.ts"
      via: "barrel import of all 8 sections"
      pattern: "from \"@/components/homepage\""
---

<objective>
Build the bottom-half homepage sections (ServicesPreview, TestimonialsGrid, EstheticianIntro, FinalCTA), create the barrel export, and wire everything into page.tsx. After this plan, the homepage is complete and renders all 8 sections at localhost:3000.

Purpose: The bottom half of the homepage converts curiosity into action — the visitor has been reassured (top half), now they see services, social proof, a human face, and a final warm CTA.
Output: 4 section components, barrel export (index.ts), and the complete page.tsx composer.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-homepage/04-RESEARCH.md
@.planning/phases/04-homepage/04-CONTEXT.md
@.planning/phases/04-homepage/04-01-SUMMARY.md
@src/components/ui/index.ts
@src/components/ui/SectionWrapper.tsx
@src/components/ui/BookingLink.tsx
@src/components/ui/StarRating.tsx
@src/components/ui/FadeUp.tsx
@src/content/services/categories.ts
@src/content/testimonials.ts
@src/content/staff.ts
@src/content/client.config.ts
@src/lib/metadata.ts
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: ServicesPreview + TestimonialsGrid</name>
  <files>src/components/homepage/ServicesPreview.tsx, src/components/homepage/TestimonialsGrid.tsx</files>
  <action>
  **ServicesPreview.tsx:**
  - Server Component (no "use client").
  - `<SectionWrapper bg="white" padding="lg">` — clean white background so the card grid reads clearly.
  - Section heading: "Our Services" (H2, Fraunces font).
  - Brief intro: "From brows to Brazilians, we've got you covered."
  - Import `serviceCategories` from `@/content/services` (the categories.ts barrel).
  - 4 category cards in a grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6`.
  - Each card: inline SVG icon matching the category iconName ("sparkles" for face, "leaf" for body, "shield" for intimate, "gift" for packages), category name (H3), tagline, and a Next.js `<Link href={/services/${category.slug}}>View Services</Link>` styled as a text link with brand-primary color and arrow indicator.
  - Write 4 inline SVGs for the icon names — simple, consistent style (24x24 viewBox, stroke-based, brand-primary color). Do NOT install an icon library.
  - Cards should have subtle hover effect (shadow lift or border highlight).
  - Wrap each card in `<FadeUp delay={i * 0.1}>` for staggered entrance.

  **TestimonialsGrid.tsx:**
  - Server Component (no "use client").
  - `<SectionWrapper bg="light" padding="lg">` — warm cream background.
  - Section heading: "What Our Clients Say" (H2, Fraunces font).
  - Import `testimonials` from `@/content/testimonials` and slice: `const featured = testimonials.slice(0, 3)`.
  - 3-card static grid: `grid grid-cols-1 md:grid-cols-3 gap-6`. NO carousel, no slider.
  - Each card: white background (`bg-white rounded-2xl p-6 shadow-sm`), `<StarRating rating={t.rating} size="sm" />`, quoted text (`"{t.quote}"`), author name (bold), service name if present (smaller, muted).
  - Wrap each card in `<FadeUp delay={i * 0.1}>` for staggered entrance.
  </action>
  <verify>
  Run `npx tsc --noEmit` — both files compile. ServicesPreview renders 4 cards with data from serviceCategories. TestimonialsGrid renders exactly 3 cards from testimonials.slice(0, 3).
  </verify>
  <done>
  ServicesPreview shows 4 category cards with icons, names, taglines, and links to /services/[slug]. TestimonialsGrid shows 3 static review cards with star ratings, quotes, and author info from the content layer.
  </done>
</task>

<task type="auto">
  <name>Task 2: EstheticianIntro + FinalCTA + barrel export + page.tsx</name>
  <files>src/components/homepage/EstheticianIntro.tsx, src/components/homepage/FinalCTA.tsx, src/components/homepage/index.ts, src/app/page.tsx</files>
  <action>
  **EstheticianIntro.tsx:**
  - Server Component (no "use client").
  - `<SectionWrapper bg="white" padding="lg">` — neutral background for the photo split layout.
  - Import `staff` from `@/content/staff`. Use `staff[0]` (Maya Chen).
  - Photo + text split layout: `grid grid-cols-1 lg:grid-cols-2 gap-12 items-center`.
  - Left column: Photo placeholder using `next/image` with `<Image src={esthetician.headshot} alt={esthetician.name} width={500} height={600} className="rounded-2xl object-cover" />`. The headshot path is in the staff data. Add a fallback gradient div in case the image doesn't load (CSS background).
  - Right column: "Meet {esthetician.name}" (H2, Fraunces), esthetician.title, bio paragraphs from `esthetician.bio`, specialties list, and a soft CTA like `<BookingLink variant="inline">Book with {esthetician.name}</BookingLink>`.
  - Wrap both columns in FadeUp.

  **FinalCTA.tsx:**
  - Server Component (no "use client").
  - `<SectionWrapper bg="primary" padding="lg">` — honey gold background for warm, high-energy close. All text white.
  - Centered layout with heading: "Ready to Experience Waxing Done Right?" (H2, Fraunces, text-white).
  - Supporting line: brief reassurance sentence.
  - `<BookingLink variant="cta">Book Your Appointment</BookingLink>` — white button styling on gold background. Add `className` override if needed for contrast (white bg, brand-primary text).
  - Trust reinforcement adjacent to CTA: `<StarRating>` with review count + "on Google" text, small and muted.
  - Import clientConfig for star rating and review count data.

  **index.ts (barrel export):**
  ```ts
  export { HomepageHero } from "./HomepageHero";
  export { TrustBar } from "./TrustBar";
  export { WhatToExpect } from "./WhatToExpect";
  export { FirstTimerSpotlight } from "./FirstTimerSpotlight";
  export { ServicesPreview } from "./ServicesPreview";
  export { TestimonialsGrid } from "./TestimonialsGrid";
  export { EstheticianIntro } from "./EstheticianIntro";
  export { FinalCTA } from "./FinalCTA";
  ```

  **page.tsx (thin composer — replaces the default Next.js scaffold):**
  - Server Component. Import `Metadata` type and `generatePageMetadata` from `@/lib/metadata`.
  - Export `metadata` using generatePageMetadata with title "Waxing Studio in Omaha | Honey & Bloom Wax Studio", description targeting first-timers and local SEO (under 155 chars), path "/".
  - Import all 8 sections from `@/components/homepage`.
  - Render them in the locked order inside a Fragment: HomepageHero, TrustBar, WhatToExpect, FirstTimerSpotlight, ServicesPreview, TestimonialsGrid, EstheticianIntro, FinalCTA.
  - No layout logic in page.tsx — it is purely a composer.
  </action>
  <verify>
  Run `npx tsc --noEmit` — all files compile with zero errors. Run `npm run dev` and visit localhost:3000 — the homepage renders all 8 sections in the correct order with no blank sections or import errors. The page scrolls through the full section flow with alternating backgrounds.
  </verify>
  <done>
  EstheticianIntro shows Maya Chen's photo placeholder and bio. FinalCTA provides a warm booking close on honey gold background with trust reinforcement. Barrel export exposes all 8 sections. page.tsx composes the complete homepage in the locked section order. The homepage at / is fully functional.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors across all files
- `npm run dev` — homepage renders at localhost:3000 with all 8 sections visible
- Section order matches CONTEXT.md locked flow: Hero -> TrustBar -> WhatToExpect -> FirstTimerSpotlight -> ServicesPreview -> TestimonialsGrid -> EstheticianIntro -> FinalCTA
- Background rhythm: dark hero -> dark trust bar -> light -> blush -> white -> light -> white -> primary
- No hardcoded business data — all values from clientConfig, services, staff, testimonials
- No "framer-motion" imports — only "motion/react"
- No carousel/slider on testimonials — static 3-card grid only
- page.tsx has generatePageMetadata with unique title and description
</verification>

<success_criteria>
- All 8 homepage section components + barrel export + page.tsx exist and compile
- Homepage renders the complete first-timer conversion funnel at localhost:3000
- Every requirement (HOME-01 through HOME-08) is addressed by a specific section component
- Background color rhythm creates clear visual separation between sections
</success_criteria>

<output>
After completion, create `.planning/phases/04-homepage/04-02-SUMMARY.md`
</output>
