---
phase: 08-contact-and-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/contact/page.tsx
  - src/components/contact/ContactForm.tsx
  - src/components/contact/ContactInfo.tsx
  - src/components/contact/index.ts
  - public/images/og-default.jpg
  - src/app/about/page.tsx
  - src/app/book/page.tsx
  - src/app/faq/page.tsx
  - src/app/page.tsx
  - src/app/services/page.tsx
autonomous: true

must_haves:
  truths:
    - "Contact page shows a 3-field form (name, phone, message) with simulated success state"
    - "Contact page shows address, phone with click-to-call, hours, and map placeholder with Google Maps link"
    - "Every page has a unique title tag under 60 characters"
    - "Every page has a unique meta description under 155 characters"
    - "Open Graph and Twitter meta tags exist on every page (verified via generatePageMetadata usage)"
  artifacts:
    - path: "src/app/contact/page.tsx"
      provides: "Contact page route with metadata and two-column layout"
      contains: "generatePageMetadata"
    - path: "src/components/contact/ContactForm.tsx"
      provides: "3-field controlled form with simulated submit"
      contains: "use client"
    - path: "src/components/contact/ContactInfo.tsx"
      provides: "Address, phone, hours, map placeholder"
      contains: "clientConfig"
    - path: "public/images/og-default.jpg"
      provides: "Default 1200x630 OG image placeholder"
  key_links:
    - from: "src/app/contact/page.tsx"
      to: "src/components/contact/ContactForm.tsx"
      via: "import and render as client component child"
      pattern: "ContactForm"
    - from: "src/components/contact/ContactInfo.tsx"
      to: "src/content/client.config.ts"
      via: "reads address, phone, hours directly"
      pattern: "clientConfig"
---

<objective>
Build the /contact page and audit metadata across all existing pages.

Purpose: The contact page is the final content page before SEO infrastructure. The metadata audit ensures every page has correct, unique title/description/OG tags before the sitemap and schema pass in Plan 02.
Output: Working /contact route, branded OG default image, all page titles verified under 60 chars.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-contact-and-seo/08-RESEARCH.md
@src/lib/metadata.ts
@src/content/client.config.ts
@src/components/ui/SectionWrapper.tsx
@src/components/ui/index.ts
@src/app/about/page.tsx
@src/app/book/page.tsx
@src/app/faq/page.tsx
@src/app/page.tsx
@src/app/services/page.tsx
@src/app/services/[slug]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contact page — ContactForm, ContactInfo, page.tsx</name>
  <files>
    src/components/contact/ContactForm.tsx
    src/components/contact/ContactInfo.tsx
    src/components/contact/index.ts
    src/app/contact/page.tsx
  </files>
  <action>
Create 4 files for the contact page:

**ContactForm.tsx** — "use client" component:
- 3 controlled fields: name (text, required), phone (tel, required), message (textarea, optional)
- `useState` for form state and `submitted` boolean
- `handleSubmit` prevents default, sets `submitted = true` (no API call — UI-only per CONTEXT.md)
- Success state: centered confirmation text "Got it! We'll get back to you within 24 hours." with a checkmark or warm message
- Inputs styled with brand tokens: border-brand-dark/20, focus:ring-brand-primary, rounded-lg, py-3 px-4
- Submit button uses brand-primary bg with white text, full width, hover:bg-brand-primary-dark
- Labels above fields in font-medium text-brand-dark

**ContactInfo.tsx** — Server Component (no "use client"):
- Imports `clientConfig` from `@/content/client.config`
- Sections: Address block, Phone (click-to-call using `tel:` link with PhoneLink component or raw `<a href="tel:...">`), Email, Hours table, Map placeholder
- Map placeholder: styled div with bg-brand-light, border, rounded-lg, ~200px height, centered address text + "View on Google Maps" external link (`https://www.google.com/maps/search/?api=1&query=` + encodeURIComponent of full address)
- Hours: iterate clientConfig.hours keys (monday through sunday), display day name + hours string in a simple two-column layout. Closed days shown in text-brand-dark/40
- Phone number prominent with large text and click-to-call

**index.ts** — Barrel export for ContactForm and ContactInfo.

**page.tsx** — Server Component:
- `generatePageMetadata` with title "Contact Us | Honey & Bloom Wax Studio" (40 chars), description "Have a question about waxing? Reach out to Honey & Bloom in Omaha. Call, visit, or send a message. We respond within 24 hours." (124 chars), path "/contact"
- Breadcrumbs with items `[{ label: "Contact", href: "/contact" }]`
- SectionWrapper bg="white" with two-column grid: `grid grid-cols-1 lg:grid-cols-2 gap-12`
- Left column: heading "Get in Touch" (H1, font-heading), subtext about reaching out, then ContactForm
- Right column: ContactInfo
- FadeUp wrapper on content sections

Follow Phase 7 pattern: page.tsx is Server Component, ContactForm is "use client" child — no escalation.
  </action>
  <verify>
Run `npx next build` — contact page should appear in the build output with no errors. Verify the route `/contact` is listed.
  </verify>
  <done>
/contact renders a two-column layout with 3-field form (name, phone, message), simulated success state, address, click-to-call phone, hours, and map placeholder with Google Maps link. Page has correct metadata.
  </done>
</task>

<task type="auto">
  <name>Task 2: OG default image + metadata audit across all pages</name>
  <files>
    public/images/og-default.jpg
    src/app/about/page.tsx
    src/app/book/page.tsx
    src/app/faq/page.tsx
    src/app/page.tsx
    src/app/services/page.tsx
  </files>
  <action>
**OG Default Image:**
Create a minimal 1200x630 branded placeholder at `public/images/og-default.jpg`. Use a Node.js script (or any CLI approach) to generate a simple image — solid brand-light (#FAF3EF) background with the studio name "Honey & Bloom Wax Studio" centered. If generating a real image is too complex, create a simple SVG converted to JPG, or use a solid-color JPEG. The key requirement: the file must exist at that path so OG tags don't reference a 404. Even a 1200x630 solid #FAF3EF rectangle is acceptable — social sharing will work.

**Metadata Audit — verify and fix these pages:**

Check every title tag is under 60 characters. Current titles to audit:

| Page | Current Title | Chars | Action |
|------|--------------|-------|--------|
| `/` | "Waxing Studio in Omaha \| Honey & Bloom Wax Studio" | 51 | OK |
| `/services` | "Waxing Services & Pricing in Omaha \| Honey & Bloom Wax Studio" | 63 | SHORTEN to "Waxing Services & Pricing \| Honey & Bloom Omaha" (49) |
| `/about` | "About Honey & Bloom Wax Studio \| Omaha Esthetician" | 52 | OK |
| `/faq` | "FAQ \| Honey & Bloom Wax Studio Omaha" | 38 | OK |
| `/book` | "Book Your Appointment \| Honey & Bloom Wax Studio Omaha" | 56 | OK |
| `/contact` | (created in Task 1) | 40 | OK |

For `/services/[slug]`, titles come from `service.metaTitle` in the data layer — do NOT modify data files here. Those titles were set in Phase 3 and are within the service data's concern. Only fix the static page metadata calls listed above.

Check every description is under 155 characters. Current descriptions:

| Page | Chars | Action |
|------|-------|--------|
| `/` | 156 | TRIM by 1-2 chars — remove "today" or rephrase last sentence to land at 154 |
| `/services` | 161 | TRIM — shorten to ~150 chars |
| `/about` | 155 | OK (borderline — verify exact count) |
| `/faq` | 104 | OK |
| `/book` | 153 | OK |

Fix only the pages that exceed limits. Keep the same keywords and CTA intent — just tighten wording.

Do NOT change `generatePageMetadata` function itself or `metadata.ts` — it already handles OG, Twitter, canonical, and robots correctly.
  </action>
  <verify>
1. Verify `public/images/og-default.jpg` exists and is a valid image file.
2. Run `npx next build` — no errors.
3. Count characters of all title and description strings to confirm compliance (titles under 60, descriptions under 155).
  </verify>
  <done>
OG default image exists at /images/og-default.jpg. All static page titles are under 60 chars. All descriptions are under 155 chars. OG and Twitter meta tags flow correctly from generatePageMetadata on every page.
  </done>
</task>

</tasks>

<verification>
1. `npx next build` completes with zero errors
2. `/contact` route appears in build output
3. All page titles verified under 60 characters
4. All page descriptions verified under 155 characters
5. `public/images/og-default.jpg` exists and is a valid image
</verification>

<success_criteria>
- Contact page renders two-column layout with form + contact info
- ContactForm shows simulated success on submit
- Click-to-call phone link works (tel: href)
- Map placeholder links to Google Maps
- Hours display correctly from clientConfig
- Every page has unique title (under 60 chars) and description (under 155 chars)
- OG default image file exists for social sharing fallback
</success_criteria>

<output>
After completion, create `.planning/phases/08-contact-and-seo/08-01-SUMMARY.md`
</output>
