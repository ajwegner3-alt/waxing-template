---
phase: 10-manual-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FUTURE_DIRECTIONS.md
autonomous: false

must_haves:
  truths:
    - "Site is live on a .vercel.app URL accessible from any device"
    - "All pages render correctly on 390px mobile viewport with no layout overflow"
    - "Sticky booking bar and click-to-call are functional on mobile"
    - "Core Web Vitals pass: LCP < 2.5s, CLS < 0.1, INP < 200ms"
    - "Schema validates on homepage, one service page, and FAQ page with no errors"
    - "All internal links resolve with no 404s"
    - "All four conversion paths work end-to-end"
    - "FUTURE_DIRECTIONS.md exists in project root"
  artifacts:
    - path: "FUTURE_DIRECTIONS.md"
      provides: "Project completion report with limitations, improvements, and tech debt"
      min_lines: 50
  key_links:
    - from: "GitHub repo"
      to: "Vercel project"
      via: "Git push auto-deploy"
      pattern: "vercel.app"
---

<objective>
Deploy the Honey & Bloom wax studio template to Vercel, run a full QA pass (automated checks first, then Andrew's manual walkthrough on iPhone), fix any issues found, generate FUTURE_DIRECTIONS.md, and push the final production build to main.

Purpose: This is the final gate before the template is considered portfolio-ready. Every page, every link, every conversion path must work on real devices.
Output: Live Vercel deployment, all checks green, FUTURE_DIRECTIONS.md committed, production build on main branch.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-manual-qa/10-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create GitHub repo and connect Vercel</name>
  <action>
Andrew needs to create the GitHub repo and connect Vercel. Claude will first rename the local branch from master to main, then Andrew handles the remote setup.

**Claude does first (automated):**
1. Run `git branch -m master main` to rename the branch
2. Run `npm run build` to confirm a clean production build with zero errors before pushing
3. If build fails, fix the issues before continuing

**Then Andrew does manually:**

### Step A: Create GitHub Repository
1. Go to https://github.com/new
2. Repository name: `waxing-template`
3. Description: `Next.js website template for waxing studios - Honey & Bloom demo`
4. Set to **Public** (this is a portfolio piece)
5. Do NOT initialize with README, .gitignore, or license (the local repo already has these)
6. Click **Create repository**
7. Copy the HTTPS URL shown (it will look like `https://github.com/YOUR-USERNAME/waxing-template.git`)

### Step B: Push Local Code to GitHub
Run these commands in the terminal (Claude will provide the exact commands after you share the repo URL):
```
git remote add origin https://github.com/YOUR-USERNAME/waxing-template.git
git push -u origin main
```

### Step C: Connect Vercel
1. Go to https://vercel.com/dashboard
2. Click **Add New... > Project**
3. Find `waxing-template` in the GitHub repo list and click **Import**
4. Framework Preset should auto-detect **Next.js** — confirm it says Next.js
5. Leave all other settings as defaults
6. Click **Deploy**
7. Wait for the build to complete (1-2 minutes)
8. Copy the deployment URL (ends in `.vercel.app`)

Share the `.vercel.app` URL with Claude to continue.
  </action>
  <verify>Andrew confirms the .vercel.app URL is live and the site loads</verify>
  <done>Site is deployed and accessible at a public .vercel.app URL</done>
</task>

<task type="auto">
  <name>Task 2: Automated QA checks</name>
  <files>FUTURE_DIRECTIONS.md</files>
  <action>
Run all automated checks against the live Vercel deployment. Fix any issues found immediately (commit, push, wait for redeploy, re-test).

**2a. Build verification**
- Run `npm run build` locally and confirm zero warnings and zero errors
- Note the total page count and bundle sizes

**2b. Internal link check**
- Use a script or CLI tool to crawl all pages and verify no 404s
- Check every internal `<a href>` and `<Link href>` resolves
- The full route list to verify (31 routes total):
  - `/` (homepage)
  - `/services` (service menu)
  - `/services/[slug]` (14 service detail pages)
  - `/about`
  - `/faq`
  - `/book`
  - `/contact`
  - `/blog`
  - `/blog/[slug]` (3 blog posts)
  - `/service-areas`
  - `/service-areas/[slug]` (6 neighborhood pages)
  - `/sitemap.xml`
  - `/robots.txt`

**2c. Lighthouse / Core Web Vitals**
- Run Lighthouse CI against the deployed homepage using `npx @lhci/cli` or `npx lighthouse` with the live URL
- Target scores: Performance >= 90, Accessibility >= 90, Best Practices >= 90, SEO >= 90
- Core Web Vitals MUST pass: LCP < 2.5s, CLS < 0.1, INP < 200ms
- If any score is below target, diagnose and fix the issue, commit, push, wait for redeploy, and re-run
- Also run Lighthouse on one service page (e.g., /services/brazilian-wax) and the /faq page

**2d. Schema validation**
- Fetch the HTML of the homepage, one service page (/services/brazilian-wax), and /faq
- Extract all `<script type="application/ld+json">` blocks
- Validate JSON-LD is well-formed (parseable JSON, correct @type values)
- Homepage should have: LocalBusiness (or BeautyBusiness), BreadcrumbList
- Service page should have: Service, BreadcrumbList
- FAQ page should have: FAQPage, BreadcrumbList
- Errors are BLOCKING. Warnings are non-blocking unless they indicate missing required fields.

**2e. Mobile viewport check (DevTools)**
- Using the build output or a headless browser check, verify no horizontal overflow at 390px viewport width
- Check that the sticky booking bar renders at the bottom of the viewport

**2f. Generate FUTURE_DIRECTIONS.md**
After all automated checks pass, create `FUTURE_DIRECTIONS.md` in the project root with these sections:
1. **Known Limitations** — What the template cannot do (e.g., booking is front-end only, no real backend, placeholder staff photo, no real Google Maps embed)
2. **Assumptions & Constraints** — Design decisions that may need revisiting (e.g., single esthetician model, Omaha-only service areas, static blog posts requiring code changes)
3. **Future Improvements** — Concrete suggestions (e.g., CMS integration, real booking API, dynamic blog, multi-location support, A/B testing for CTAs)
4. **Technical Debt** — Shortcuts taken (e.g., hardcoded calendar data, placeholder images, any TODO comments in code)

Commit FUTURE_DIRECTIONS.md and push to trigger a redeploy.
  </action>
  <verify>
- `npm run build` exits 0 with no errors
- All 31 routes return 200 (not 404)
- Lighthouse performance >= 90 on homepage
- Core Web Vitals all pass (LCP < 2.5s, CLS < 0.1, INP < 200ms)
- JSON-LD validates on 3 pages with correct @type values
- FUTURE_DIRECTIONS.md exists and is committed
  </verify>
  <done>All automated checks pass, any issues found are fixed and redeployed, FUTURE_DIRECTIONS.md is committed and pushed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Andrew's manual walkthrough on iPhone and desktop</name>
  <what-built>The complete Honey & Bloom wax studio template, deployed live on Vercel. All automated checks have passed (links, Lighthouse, schema, Core Web Vitals). Now it needs a real human eye on a real device.</what-built>
  <how-to-verify>

### Part A: iPhone Safari Testing (primary mobile test)

Open the .vercel.app URL on your iPhone in Safari. For each check, note "pass" or describe the issue.

**Global elements (test on every page you visit):**
1. Header logo and nav hamburger menu are visible, not overlapping content
2. Tap the hamburger — mobile nav drawer opens smoothly, links are tappable (44px+ targets)
3. Sticky booking bar is visible at the bottom without scrolling — "Book Now" and phone icon are both tappable
4. Tap the phone icon — your iPhone should open the dialer with the studio phone number pre-filled
5. No horizontal scroll on any page (drag left/right — page should not move)

**Homepage (/):**
6. Hero section fills the viewport, headline is readable, both CTAs are visible
7. Scroll down — all sections load (trust bar, what to expect, first-timer spotlight, services, testimonials, esthetician intro, final CTA)
8. Tap "Book Now" in the hero — takes you to /book

**Booking flow (/book):**
9. Progress indicator shows step 1 of 4
10. Select a service — advances to step 2
11. Select esthetician preference — advances to step 3
12. Tap a date, then a time slot — advances to step 4 (confirmation)
13. Confirmation shows the correct service, date, time, prep reminders, and cancellation policy
14. "Complete Booking" button is visible and tappable

**Services (/services):**
15. Category pills are visible and horizontally scrollable
16. Service cards show name, price, duration — tap one to go to its detail page
17. On a service detail page: hero, prep, aftercare, FAQ accordion, related services, and CTA all render
18. FAQ accordion items expand and collapse on tap

**Contact (/contact):**
19. Form fields (name, phone, message) are tappable and the keyboard opens
20. Phone number is click-to-call
21. Map placeholder and hours are visible

**Blog (/blog):**
22. Blog index shows cards for all 3 posts
23. Tap a post — full article renders with readable typography

**Service Areas (/service-areas):**
24. Index page shows neighborhood cards
25. Tap one — area page shows localized content and service links

### Part B: Desktop Chrome Testing

Open the same URL on your desktop in Chrome.

26. Resize browser window from full-width down to ~768px — layout responds smoothly, no broken elements
27. At full desktop width: header shows all nav items + "Book Now" button, no hamburger
28. Sticky booking bar is NOT visible on desktop (should be mobile-only)
29. Quick-scan all pages for visual polish — fonts look right, spacing is consistent, no broken images or placeholder text showing through

### Part C: Overall Polish Check

30. Does this look like a portfolio piece you would show a potential client? Note anything that feels off, unfinished, or unprofessional.

  </how-to-verify>
  <resume-signal>Type "approved" if everything passes, or list the issues found (e.g., "Issue: booking bar overlaps content on iPhone SE, FAQ accordion arrow doesn't rotate"). Claude will fix each issue, commit, push, and re-test.</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. Every automated check passed (build, links, Lighthouse, schema, CWV)
2. Andrew confirmed manual walkthrough passes on iPhone and desktop
3. FUTURE_DIRECTIONS.md is committed to the repo
4. Final production build is on the `main` branch on GitHub
5. Vercel auto-deployed the latest main — live URL works
</verification>

<success_criteria>
- Live Vercel deployment accessible at .vercel.app URL
- Zero 404s across all 31 routes
- Lighthouse Performance >= 90 on homepage, service page, and FAQ page
- Core Web Vitals: LCP < 2.5s, CLS < 0.1, INP < 200ms (all three)
- Schema validates on homepage (BeautyBusiness), service page (Service), FAQ page (FAQPage)
- All four conversion paths confirmed functional (booking flow, click-to-call, service menu to detail to CTA, contact form)
- No layout overflow on 390px viewport
- FUTURE_DIRECTIONS.md committed with all four required sections
- Andrew signs off on visual polish and mobile experience
- Final build pushed to main on GitHub
</success_criteria>

<output>
After completion, create `.planning/phases/10-manual-qa/10-01-SUMMARY.md`
</output>
