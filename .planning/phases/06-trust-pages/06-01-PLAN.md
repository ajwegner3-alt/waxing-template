---
phase: 06-trust-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/trust/AboutHero.tsx
  - src/components/trust/BrandStory.tsx
  - src/components/trust/EstheticianProfile.tsx
  - src/components/trust/PhilosophyValues.tsx
  - src/components/trust/HygieneProtocols.tsx
  - src/components/trust/AboutReviews.tsx
  - src/components/trust/FAQCategorySection.tsx
  - src/components/trust/TrustCTA.tsx
  - src/components/trust/index.ts
  - src/app/about/page.tsx
autonomous: true

must_haves:
  truths:
    - "About page tells the studio origin story and comfort-first philosophy"
    - "Full esthetician profile shows all 3 bio paragraphs, all specialties, and all certifications"
    - "Hygiene protocols section displays scannable icon checklist of sanitation practices"
    - "Google reviews section shows aggregate rating, review count, Google link, and 3 individual review cards"
    - "Trust signals (rating, licensed, hygiene) appear inline adjacent to the booking CTA"
  artifacts:
    - path: "src/components/trust/index.ts"
      provides: "Barrel export for all trust components"
      exports: ["AboutHero", "BrandStory", "EstheticianProfile", "PhilosophyValues", "HygieneProtocols", "AboutReviews", "FAQCategorySection", "TrustCTA"]
    - path: "src/app/about/page.tsx"
      provides: "About page route with metadata"
      exports: ["metadata", "default"]
    - path: "src/components/trust/EstheticianProfile.tsx"
      provides: "Full esthetician bio, specialties, certifications"
      contains: "esthetician.bio"
    - path: "src/components/trust/AboutReviews.tsx"
      provides: "Aggregate rating + 3 testimonial cards"
      contains: "StarRating"
    - path: "src/components/trust/TrustCTA.tsx"
      provides: "Reusable warm CTA section with inline trust badges"
      contains: "BookingLink"
  key_links:
    - from: "src/app/about/page.tsx"
      to: "src/components/trust/index.ts"
      via: "barrel import"
      pattern: "from \"@/components/trust\""
    - from: "src/components/trust/EstheticianProfile.tsx"
      to: "src/content/staff.ts"
      via: "data import"
      pattern: "import.*staff.*from.*@/content/staff"
    - from: "src/components/trust/AboutReviews.tsx"
      to: "src/content/testimonials.ts"
      via: "data import"
      pattern: "import.*testimonials.*from.*@/content/testimonials"
    - from: "src/components/trust/TrustCTA.tsx"
      to: "src/components/ui"
      via: "BookingLink import"
      pattern: "import.*BookingLink.*from.*@/components/ui"
---

<objective>
Build all trust section components and the About page.

Purpose: The About page is the primary trust-building destination for visitors who need more reassurance before booking. It tells the studio's story, introduces the esthetician in full detail, showcases hygiene standards, displays Google reviews, and ends with a warm booking CTA with inline trust badges.

Output: 8 new Server Components in `src/components/trust/`, barrel export, and a complete `/about` route.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-trust-pages/06-CONTEXT.md
@.planning/phases/06-trust-pages/06-RESEARCH.md
@src/components/homepage/FinalCTA.tsx
@src/components/homepage/TestimonialsGrid.tsx
@src/components/homepage/EstheticianIntro.tsx
@src/components/services/FAQAccordion.tsx
@src/components/ui/Badge.tsx
@src/components/ui/StarRating.tsx
@src/components/ui/SectionWrapper.tsx
@src/components/ui/FadeUp.tsx
@src/components/ui/index.ts
@src/components/layout/Breadcrumbs.tsx
@src/content/staff.ts
@src/content/testimonials.ts
@src/content/client.config.ts
@src/content/faqs.ts
@src/lib/types.ts
@src/lib/metadata.ts
@src/app/page.tsx
@src/app/services/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trust components — AboutHero, BrandStory, EstheticianProfile, PhilosophyValues, HygieneProtocols</name>
  <files>
    src/components/trust/AboutHero.tsx
    src/components/trust/BrandStory.tsx
    src/components/trust/EstheticianProfile.tsx
    src/components/trust/PhilosophyValues.tsx
    src/components/trust/HygieneProtocols.tsx
  </files>
  <action>
Create 5 Server Components in `src/components/trust/`. No `"use client"` on any of them.

**AboutHero.tsx** — Compact branded page header (NOT a tall image hero). Use `SectionWrapper bg="light" padding="md"`. Add `Breadcrumbs` from `@/components/layout` with `items={[{ label: "About", href: "/about" }]}` and `className="mb-6"`. Then FadeUp wrapping: eyebrow label "Our Story" (text-brand-secondary font-semibold text-sm tracking-wide uppercase mb-3), H1 "A Studio Built Around You" (font-heading text-4xl lg:text-5xl font-semibold text-brand-dark mb-4), and tagline paragraph using `clientConfig.tagline` from `@/content/client.config` (text-brand-dark/65 text-lg leading-relaxed). Max width container: max-w-2xl.

**BrandStory.tsx** — Origin story section. Use `SectionWrapper bg="white" padding="md"`. FadeUp wrapping max-w-3xl mx-auto prose content. Write 2-3 paragraphs of origin story copy: how Maya started Honey & Bloom after seeing how clinical and intimidating most waxing studios felt — she wanted to create a space where first-timers could walk in nervous and leave wondering why they waited so long. Mention the honey-based hard wax formula, the warm studio aesthetic, and the Omaha roots. Use font-heading for section heading "How It Started" (text-2xl lg:text-3xl font-semibold text-brand-dark mb-6). Body paragraphs: text-brand-dark/75 leading-relaxed text-base, with mb-4 between paragraphs.

**EstheticianProfile.tsx** — Full esthetician profile. Use `SectionWrapper bg="light" padding="lg"`. Import `staff` from `@/content/staff`, get `staff[0]`. Two-column layout on desktop (lg:grid-cols-2 gap-12 items-start), stacked on mobile. Left column: image placeholder div (aspect-[3/4] bg-brand-primary/10 rounded-2xl with centered text "Photo" — same placeholder pattern used elsewhere). Right column: H2 with esthetician name (font-heading text-3xl font-semibold text-brand-dark mb-2), role subtitle (text-brand-secondary font-medium mb-4), then ALL bio paragraphs (esthetician.bio — do NOT use .slice()), each as a `<p>` with mb-4 text-brand-dark/75 leading-relaxed. Below bio: specialties as pills (bg-brand-primary/10 text-brand-dark/75 font-medium px-3 py-1 rounded-full text-sm) in a flex-wrap gap-2 container with "Specialties" label above. Below that: certifications using `Badge variant="trust"` from `@/components/ui`. Include years experience as a subtle detail: `${esthetician.yearsExperience}+ years experience` in text-brand-dark/50.

**PhilosophyValues.tsx** — 4 brand value icon cards. Use `SectionWrapper bg="blush" padding="lg"`. Centered H2 "What We Believe" (font-heading text-3xl lg:text-4xl font-semibold text-brand-dark text-center mb-10). 4-column grid on lg (lg:grid-cols-4), 2 on md (md:grid-cols-2), 1 on mobile. Each card: bg-white rounded-2xl border border-brand-primary/10 p-6 text-center. Icon container: w-12 h-12 mx-auto bg-brand-primary/10 rounded-xl flex items-center justify-center mb-4. Use inline SVG icons (no icon library):
  - Comfort First: Heart icon path (`M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z`)
  - Natural Ingredients: Leaf icon (design a simple leaf path or use `M17 8C8 10 5.9 16.17 3.82 21.34l1.89.66.95-2.3c.48.17.98.3 1.34.3C19 20 22 15 22 9c0-6-5-7-5-7S17 8 17 8z`)
  - Transparency: Eye icon (`M12 4.5C7 4.5 2.73 7.61 1 12c1.73 4.39 6 7.5 11 7.5s9.27-3.11 11-7.5c-1.73-4.39-6-7.5-11-7.5zM12 17c-2.76 0-5-2.24-5-5s2.24-5 5-5 5 2.24 5 5-2.24 5-5 5zm0-8c-1.66 0-3 1.34-3 3s1.34 3 3 3 3-1.34 3-3-1.34-3-3-3z`)
  - Judgment-Free: Shield icon (`M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4z`)
Each card: value name as H3 (font-heading text-lg font-semibold text-brand-dark mb-2), 1-2 sentence description as p (text-brand-dark/65 text-sm leading-relaxed). Descriptions:
  - "Every detail is designed around reducing first-timer anxiety — from the warm studio to Maya walking you through each step."
  - "Our signature honey-based hard wax is gentle, effective, and free of parabens, harsh resins, and artificial fragrances."
  - "No surprises, ever. Maya explains exactly what's happening before, during, and after every service."
  - "All bodies welcome, all experience levels, zero awkwardness. This is a judgment-free zone, always."
Wrap each card in FadeUp with staggered delay (i * 0.1).

**HygieneProtocols.tsx** — Icon checklist. Use `SectionWrapper bg="white" padding="md"`. Max-w-3xl mx-auto. H2 "Our Hygiene Standards" (font-heading text-2xl lg:text-3xl font-semibold text-brand-dark mb-3). Intro paragraph: "Your skin's safety is non-negotiable. Here's exactly what we do for every single appointment." (text-brand-dark/65 leading-relaxed mb-8). Static array of 6 items:
  1. "Single-use applicators — never double-dipped"
  2. "Fresh wax melted for each client, every time"
  3. "Hospital-grade surface sanitization between appointments"
  4. "Disposable gloves changed for each service"
  5. "Fresh linens for every appointment"
  6. "Licensed and inspected — exceeding Nebraska state standards"
Render each as flex items-start gap-3 mb-4. Inline SVG checkmark icon (w-5 h-5 text-brand-secondary flex-shrink-0 mt-0.5, path: `M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z`). Text: text-brand-dark/80 text-sm leading-relaxed. Wrap the list in FadeUp.
  </action>
  <verify>Run `npx tsc --noEmit` — no TypeScript errors. Visually confirm each component file exists in src/components/trust/ and has no "use client" directive.</verify>
  <done>5 Server Components created in src/components/trust/ with correct imports from existing UI primitives and content files. No TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: AboutReviews, FAQCategorySection, TrustCTA, barrel export, and About page route</name>
  <files>
    src/components/trust/AboutReviews.tsx
    src/components/trust/FAQCategorySection.tsx
    src/components/trust/TrustCTA.tsx
    src/components/trust/index.ts
    src/app/about/page.tsx
  </files>
  <action>
Create the remaining 3 components, barrel export, and About page.

**AboutReviews.tsx** — Aggregate rating + 3 testimonial cards. Server Component (no "use client"). Use `SectionWrapper bg="light" padding="lg"`. Import `clientConfig` from `@/content/client.config`, `testimonials` from `@/content/testimonials`, `StarRating` from `@/components/ui`, `FadeUp` from `@/components/ui`. H2 "What Our Clients Say" (font-heading text-3xl font-semibold text-brand-dark mb-2). Below heading: aggregate rating row — flex flex-wrap items-center gap-4 mb-10. Use `<StarRating rating={clientConfig.reviewAverage} size="lg" showValue reviewCount={clientConfig.reviewCount} />`. Next to it: `<a href={clientConfig.googleBusinessUrl} target="_blank" rel="noopener noreferrer">Read all reviews on Google &rarr;</a>` styled as text-brand-primary font-semibold text-sm hover:underline transition-colors. Below: 3-column grid (grid-cols-1 md:grid-cols-3 gap-6). Select first 3 testimonials (`testimonials.slice(0, 3)` — these are first-timer relief stories per the data). Each card: EXACT same markup as TestimonialsGrid.tsx cards — `<figure>` with bg-white rounded-2xl border border-brand-primary/10 p-6 gap-4 h-full shadow-sm. Inside: `<StarRating rating={t.rating} size="sm" />`, `<blockquote>` with quote text, `<figcaption>` with name and service. Copy the card structure from TestimonialsGrid.tsx exactly for visual consistency. Wrap each card in FadeUp with delay={i * 0.1}.

**FAQCategorySection.tsx** — Server Component. Props: `{ heading: string; intro: string; faqs: FAQ[]; bg?: "white" | "light" }`. Import `FAQ` type from `@/lib/types`, `SectionWrapper` and `FadeUp` from `@/components/ui`, `FAQAccordion` from `@/components/services`. Render: `SectionWrapper` with the bg prop (default "white") and padding="md". Inside FadeUp: max-w-3xl mx-auto. H2 with heading (font-heading text-2xl lg:text-3xl font-semibold text-brand-dark mb-3). Paragraph with intro (text-brand-dark/65 leading-relaxed mb-6). Then `<FAQAccordion faqs={faqs} />`. Note: use H2 (not H3 as the research said) — these are top-level sections on the FAQ page.

**TrustCTA.tsx** — Reusable warm CTA. Server Component. Props: `{ page: "about" | "faq" }`. Use `SectionWrapper bg="primary" padding="lg"`. Center everything (text-center). Headline varies by page prop:
  - about: "Ready to Experience the Difference?"
  - faq: "Still Have Questions? We'd Love to Help"
Subtitle varies:
  - about: "Book your first appointment and see why Omaha trusts Honey & Bloom."
  - faq: "Or skip the reading and book — we'll walk you through everything in person."
H2 heading: font-heading text-3xl lg:text-4xl font-semibold text-white mb-4. Subtitle: text-white/80 text-lg leading-relaxed mb-8 max-w-2xl mx-auto. Below: flex flex-col sm:flex-row items-center justify-center gap-4. `<BookingLink className="!bg-white !text-brand-dark hover:!bg-white/90" />` and `<PhoneLink variant="ghost" className="!text-white !border-white/30" />`. Below CTAs: inline trust badge row — flex flex-wrap items-center justify-center gap-3 mt-6. Three badges separated by dots: "{reviewAverage} stars . {reviewCount}+ reviews", "Licensed Esthetician", "Single-use applicators". All text-white/70 text-sm. Dot separators: text-white/40 text-xs. Use inline SVG star icon (w-4 h-4) before the rating text. Wrap content in FadeUp.

**index.ts** — Barrel export all 8 components:
```
export { AboutHero } from "./AboutHero";
export { BrandStory } from "./BrandStory";
export { EstheticianProfile } from "./EstheticianProfile";
export { PhilosophyValues } from "./PhilosophyValues";
export { HygieneProtocols } from "./HygieneProtocols";
export { AboutReviews } from "./AboutReviews";
export { FAQCategorySection } from "./FAQCategorySection";
export { TrustCTA } from "./TrustCTA";
```

**app/about/page.tsx** — Thin composer. Server Component. Export `metadata` using `generatePageMetadata({ title: "About Honey & Bloom | Wax Studio in Omaha", description: "Meet Maya Chen, Omaha's first-timer waxing specialist. Learn about our honey-based wax formula, comfort-first philosophy, and hygiene standards.", path: "/about" })`. Default export renders all 7 sections in order: AboutHero, BrandStory, EstheticianProfile, PhilosophyValues, HygieneProtocols, AboutReviews, TrustCTA with page="about". Import all from `@/components/trust` barrel.
  </action>
  <verify>Run `npx tsc --noEmit` — no TypeScript errors. Run `npm run build` — About page builds successfully. Navigate to http://localhost:3000/about and confirm all 7 sections render in order with real content (not placeholder text). Confirm aggregate rating shows 4.9 stars and 247+ reviews. Confirm 3 testimonial cards display. Confirm TrustCTA has booking button and inline trust badges.</verify>
  <done>About page at /about renders with brand story, full esthetician profile (3 bio paragraphs, all specialties, 2 certifications), 4 philosophy value cards, hygiene checklist, aggregate Google rating with 3 review cards, and warm booking CTA with inline trust badges. All components are Server Components except BookingLink/PhoneLink leaf nodes. Barrel export provides all 8 trust components.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run build` succeeds — About page statically generated
- `/about` renders all 7 sections in correct order
- EstheticianProfile shows all 3 bio paragraphs (not 2)
- AboutReviews shows aggregate 4.9 stars, 247+ reviews, Google link, 3 cards
- HygieneProtocols shows 6 checklist items with checkmark icons
- PhilosophyValues shows 4 icon cards in responsive grid
- TrustCTA shows BookingLink + PhoneLink + inline trust badges
- Breadcrumbs appear at top of About page
- No "use client" on any trust component except leaf UI primitives
</verification>

<success_criteria>
1. /about page is accessible and renders all 7 sections with real content
2. Full esthetician bio (3 paragraphs), all specialties, all certifications visible
3. Aggregate Google rating + 3 individual review cards display correctly
4. Hygiene checklist is scannable with checkmark icons
5. TrustCTA has booking button with inline trust badges (rating, licensed, hygiene)
6. All 8 trust components exported from barrel at @/components/trust
7. Zero TypeScript errors, successful build
</success_criteria>

<output>
After completion, create `.planning/phases/06-trust-pages/06-01-SUMMARY.md`
</output>
