---
phase: 06-trust-pages
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/faq/page.tsx
autonomous: true

must_haves:
  truths:
    - "FAQ page displays 16 questions organized by 7 anxiety categories"
    - "Each category has an H2 heading and intro paragraph before the accordions"
    - "Accordion UI reuses FAQAccordion from Phase 5 with native details/summary"
    - "FAQ page ends with warm booking CTA and inline trust badges"
    - "Alternating backgrounds distinguish adjacent category sections"
  artifacts:
    - path: "src/app/faq/page.tsx"
      provides: "FAQ page route with metadata and 7 category sections"
      exports: ["metadata", "default"]
      contains: "FAQ_CATEGORIES"
  key_links:
    - from: "src/app/faq/page.tsx"
      to: "src/content/faqs.ts"
      via: "data import"
      pattern: "import.*faqs.*from.*@/content/faqs"
    - from: "src/app/faq/page.tsx"
      to: "src/components/trust"
      via: "barrel import"
      pattern: "from \"@/components/trust\""
    - from: "src/app/faq/page.tsx"
      to: "src/components/services"
      via: "FAQAccordion reuse through FAQCategorySection"
      pattern: "FAQCategorySection"
---

<objective>
Build the FAQ page with 7 anxiety category sections and accordion UI.

Purpose: The FAQ page answers every first-timer anxiety question before they need to ask — organized by the exact worries that prevent booking (pain, privacy, hygiene, prep, aftercare, sensitive skin). This is the final reassurance page before a visitor decides to book.

Output: A complete `/faq` route with 16 FAQs across 7 stacked category sections, reusing FAQAccordion from Phase 5 and TrustCTA from Plan 01.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-trust-pages/06-CONTEXT.md
@.planning/phases/06-trust-pages/06-RESEARCH.md
@.planning/phases/06-trust-pages/06-01-SUMMARY.md
@src/content/faqs.ts
@src/lib/types.ts
@src/lib/metadata.ts
@src/components/trust/FAQCategorySection.tsx
@src/components/trust/TrustCTA.tsx
@src/components/trust/index.ts
@src/components/services/FAQAccordion.tsx
@src/components/ui/SectionWrapper.tsx
@src/components/ui/FadeUp.tsx
@src/components/layout/Breadcrumbs.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: FAQ page route with 7 category sections</name>
  <files>src/app/faq/page.tsx</files>
  <action>
Create `src/app/faq/page.tsx` as a Server Component (no "use client"). Follow the thin-composer pattern from `app/page.tsx` and `app/services/page.tsx`.

**Metadata:** Export `metadata` using `generatePageMetadata({ title: "FAQ | Honey & Bloom Wax Studio Omaha", description: "Answers to every first-timer waxing question — pain, prep, privacy, hygiene, aftercare, and sensitive skin. Read before your appointment.", path: "/faq" })`.

**Page header section:** Use `SectionWrapper bg="light" padding="md"`. Inside: `Breadcrumbs` from `@/components/layout` with `items={[{ label: "FAQ", href: "/faq" }]}` and `className="mb-6"`. Then FadeUp wrapping max-w-2xl container: H1 "Your Questions, Answered" (font-heading text-4xl lg:text-5xl font-semibold text-brand-dark mb-4). Subtitle paragraph: "First-timer nerves are real. We've answered the questions we hear most — organized by exactly what's on your mind." (text-brand-dark/65 text-lg leading-relaxed).

**Category metadata array:** Define `FAQ_CATEGORIES` as a static const array at module level (NOT in faqs.ts — this is page-level presentation). 7 entries, each with `key` (matching the FAQ category union type), `heading`, and `intro`:

1. key: "first-timer", heading: "First-Timer Questions", intro: "Never been waxed before? You're in the right place. Here's everything you need to know before your first appointment."
2. key: "pain", heading: "Pain & Comfort", intro: "Yes, waxing involves a quick sting — but it's usually far less than people expect. Here's the honest truth about what it feels like."
3. key: "prep", heading: "How to Prepare", intro: "A little prep goes a long way for your comfort and results. Here's what to do (and skip) before you come in."
4. key: "privacy", heading: "Privacy & Comfort", intro: "Every service happens in a fully private room. Here's exactly what to expect so nothing catches you off guard."
5. key: "hygiene", heading: "Hygiene & Safety", intro: "Your skin's safety is non-negotiable. Here's how we keep every appointment clean and professional."
6. key: "aftercare", heading: "Aftercare", intro: "What you do after your wax matters just as much as the wax itself. These tips will keep your skin smooth and happy."
7. key: "sensitive-skin", heading: "Sensitive Skin", intro: "Reactive skin is welcome here. Our honey-based hard wax is formulated specifically for clients who've had trouble elsewhere."

**Category sections:** Map over FAQ_CATEGORIES. For each, filter `faqs` by `f.category === cat.key`. If filtered array is empty, return null. Otherwise render `<FAQCategorySection key={cat.key} heading={cat.heading} intro={cat.intro} faqs={categoryFaqs} bg={i % 2 === 0 ? "white" : "light"} />`. This gives alternating white/light backgrounds.

**Footer CTA:** After all category sections, render `<TrustCTA page="faq" />`.

**TODO comment:** Add `{/* TODO Phase 8: Add FAQPage schema via SchemaScript */}` at the top of the returned JSX, before the first SectionWrapper.

Import `faqs` from `@/content/faqs`. Import `FAQCategorySection` and `TrustCTA` from `@/components/trust`. Import `SectionWrapper`, `FadeUp` from `@/components/ui`. Import `Breadcrumbs` from `@/components/layout`. Import `generatePageMetadata` from `@/lib/metadata`. Import `type Metadata` from `next`.
  </action>
  <verify>Run `npx tsc --noEmit` — no TypeScript errors. Run `npm run build` — FAQ page builds successfully. Navigate to http://localhost:3000/faq and confirm: (1) page header with H1 and breadcrumbs, (2) all 7 category sections render with headings and intro paragraphs, (3) 16 FAQ accordions total across all sections, (4) alternating white/light backgrounds, (5) TrustCTA at bottom with booking button and trust badges. Run `grep -c "details" src/app/faq/page.tsx` — should be 0 (FAQ rendering is delegated to FAQAccordion via FAQCategorySection, not inline).</verify>
  <done>FAQ page at /faq displays 16 questions across 7 anxiety categories with H2 headings, intro paragraphs, alternating backgrounds, native accordion UI, breadcrumbs, and warm booking CTA with inline trust badges. All rendering uses existing FAQAccordion component. No "use client" — entire page is a Server Component.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run build` succeeds — FAQ page statically generated
- `/faq` renders page header with H1 "Your Questions, Answered" and breadcrumbs
- All 7 anxiety categories render as distinct sections with H2 + intro + accordions
- 16 total FAQ items visible across all sections (click to expand/collapse)
- Alternating white/light backgrounds distinguish adjacent sections
- TrustCTA at bottom shows BookingLink + inline trust badges
- No empty category sections render (guard in place)
- FAQ_CATEGORIES defined at page level, not in faqs.ts
- TODO comment for Phase 8 FAQPage schema is present
</verification>

<success_criteria>
1. /faq page accessible with correct metadata (title, description)
2. 7 category sections visible with descriptive headings and intro paragraphs
3. 16 FAQ accordions expand/collapse using native details/summary
4. Alternating section backgrounds provide visual rhythm
5. Breadcrumbs at top, TrustCTA with trust badges at bottom
6. Zero TypeScript errors, successful build
</success_criteria>

<output>
After completion, create `.planning/phases/06-trust-pages/06-02-SUMMARY.md`
</output>
