---
phase: 03-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/services/face.ts
  - src/content/services/body.ts
  - src/content/services/intimate.ts
  - src/content/services/packages.ts
  - src/content/services/categories.ts
  - src/content/services/index.ts
autonomous: true

must_haves:
  truths:
    - "14 services exist across 4 categories (face, body, intimate, packages) with no TypeScript errors"
    - "Every service has pricing, duration, painLevel, preparation[], aftercare[], and ingredients[] populated with real content"
    - "getServiceBySlug, getServicesByCategory, getCategoryBySlug accessor functions return correct results"
    - "relatedServices slugs on every service resolve to actual services (no dangling references)"
    - "ServiceCategory.serviceSlugs are derived from actual service arrays, not manually typed"
  artifacts:
    - path: "src/content/services/face.ts"
      provides: "4 face waxing services (eyebrow, upper lip, chin, full face)"
      exports: ["faceServices", "FACE_SLUGS"]
    - path: "src/content/services/body.ts"
      provides: "5 body waxing services (underarm, full arm, half leg, full leg, back)"
      exports: ["bodyServices", "BODY_SLUGS"]
    - path: "src/content/services/intimate.ts"
      provides: "3 intimate waxing services (bikini, extended bikini, Brazilian)"
      exports: ["intimateServices", "INTIMATE_SLUGS"]
    - path: "src/content/services/packages.ts"
      provides: "2 packages (first-timer, smooth all over)"
      exports: ["packageServices", "PACKAGE_SLUGS"]
    - path: "src/content/services/categories.ts"
      provides: "4 ServiceCategory objects with serviceSlugs derived from service arrays"
      exports: ["serviceCategories"]
    - path: "src/content/services/index.ts"
      provides: "Barrel exports and accessor functions"
      exports: ["allServices", "getServiceBySlug", "getServicesByCategory", "getCategoryBySlug", "serviceCategories"]
  key_links:
    - from: "src/content/services/index.ts"
      to: "src/content/services/face.ts, body.ts, intimate.ts, packages.ts"
      via: "import and spread into allServices array"
      pattern: "\\.\\.\\.(faceServices|bodyServices|intimateServices|packageServices)"
    - from: "src/content/services/categories.ts"
      to: "src/content/services/face.ts, body.ts, intimate.ts, packages.ts"
      via: "import service arrays to derive serviceSlugs"
      pattern: "serviceSlugs:.*\\.map\\(.*\\.slug\\)"
    - from: "each Service.relatedServices"
      to: "other Service.slug values"
      via: "string slug cross-references"
      pattern: "relatedServices:"
---

<objective>
Create the complete service catalog data layer — 14 waxing services across 4 categories with all required fields, plus accessor functions for downstream page consumption.

Purpose: Phase 4 (Homepage), Phase 5 (Service Pages), and Phase 7 (Booking) all import from this service catalog. It is the highest-dependency data file in the project.
Output: 6 files in src/content/services/ — 4 category data files, 1 categories definition, 1 barrel index with accessor functions.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-data-layer/03-RESEARCH.md
@.planning/phases/03-data-layer/03-CONTEXT.md
@src/lib/types.ts
@src/content/client.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 service category data files with slug constants</name>
  <files>
    src/content/services/face.ts
    src/content/services/body.ts
    src/content/services/intimate.ts
    src/content/services/packages.ts
  </files>
  <action>
Create the `src/content/services/` directory and 4 category data files. Each file follows the same pattern:

1. Import `type { Service } from "@/lib/types"`
2. Export a slug constants object (e.g., `FACE_SLUGS`) using `as const` — this prevents typos in relatedServices cross-references
3. Export a typed `Service[]` array with all fields populated

**Slug map (14 total services):**

| File | Slugs |
|------|-------|
| face.ts | eyebrow-wax, upper-lip-wax, chin-wax, full-face-wax |
| body.ts | underarm-wax, full-arm-wax, half-leg-wax, full-leg-wax, back-wax |
| intimate.ts | bikini-wax, extended-bikini-wax, brazilian-wax |
| packages.ts | first-timer-package, smooth-all-over-package |

**Content rules (from CONTEXT.md decisions):**
- Warm conversational tone, second-person ("you/your")
- Service descriptions: short, 2-3 sentences — let prep/aftercare carry detail
- Exact dollar prices (not ranges) — e.g., `price: 22`, `priceDisplay: "$22"`
- Pain level: numeric 1-5 (PainLevel type) — display layer adds friendly labels
- sensitiveSkintSafe: boolean based on the body area
- preparation[]: 3-5 bullets tailored to the specific body area (Brazilian prep differs from eyebrow prep)
- aftercare[]: 3-5 bullets tailored to the specific body area
- ingredients[]: honey-based hard wax is the base; vary secondary ingredients by service
- whatToExpect: 2-4 sentences, first-person narrative feel, anxiety-reducing for first-timers
- faqs: 1-2 inline FAQs per service (these are service-specific, separate from the global FAQ dataset)
- relatedServices: use slug constants from other files (import FACE_SLUGS, BODY_SLUGS, etc.) — minimum 2 per service
- metaTitle: "[Service] in Omaha | Honey & Bloom Wax Studio" — under 60 chars
- metaDescription: 150-155 chars, include primary keyword + CTA
- coverImage: `/images/services/[slug].jpg` convention (no actual image files)
- heroHeadline and heroSubheadline: unique per service, anxiety-reducing tone

**First-Timer Package (packages.ts):**
- slug: "first-timer-package", categorySlug: "packages"
- price: 70 (Brazilian $65 + eyebrow $22 = $87 a-la-carte, $17 savings)
- Richer whatToExpect and shortDescription than standard services
- Mention complimentary aftercare kit in description
- heroHeadline speaks directly to first-timer anxiety

**Cross-reference pattern for relatedServices:**
```typescript
// At top of each file, import slug constants from other files
import { BODY_SLUGS } from "./body";
import { INTIMATE_SLUGS } from "./intimate";

// In relatedServices arrays:
relatedServices: [BODY_SLUGS.fullLeg, INTIMATE_SLUGS.bikini],
```

Note: Since files cross-reference each other, write all 4 files in one pass. TypeScript may show circular import warnings — these are fine for `as const` objects (they resolve at compile time, no runtime circularity).

Run `npx tsc --noEmit` after creating all 4 files to confirm all 14 Service objects satisfy the interface.
  </action>
  <verify>
Run `npx tsc --noEmit` — zero TypeScript errors. Visually confirm each file exports a SLUGS constant and a typed Service[] array. Count: 4 face + 5 body + 3 intimate + 2 packages = 14 services.
  </verify>
  <done>14 services across 4 files compile without errors, every required Service field is populated with real content (no placeholder/lorem), and slug constants are exported for cross-referencing.</done>
</task>

<task type="auto">
  <name>Task 2: Create categories.ts and index.ts barrel with accessor functions</name>
  <files>
    src/content/services/categories.ts
    src/content/services/index.ts
  </files>
  <action>
**categories.ts:**

Import all 4 service arrays (faceServices, bodyServices, intimateServices, packageServices) from their respective files. Define `serviceCategories: ServiceCategory[]` with 4 entries:

| slug | name | serviceSlugs derivation |
|------|------|------------------------|
| face | Face Waxing | `faceServices.map(s => s.slug)` |
| body | Body Waxing | `bodyServices.map(s => s.slug)` |
| intimate | Intimate Waxing | `intimateServices.map(s => s.slug)` |
| packages | Packages | `packageServices.map(s => s.slug)` |

CRITICAL: serviceSlugs MUST be derived from the actual service arrays using `.map(s => s.slug)` — never manually typed. This prevents the sync pitfall from RESEARCH.md.

Each category needs: slug, name, tagline (short marketing phrase), description (paragraph), coverImage (`/images/categories/[slug].jpg`), iconName (descriptive identifier like "sparkles" or "leaf" — for future icon mapping).

**index.ts:**

Import all 4 service arrays and serviceCategories. Export:

```typescript
export const allServices: Service[] = [
  ...faceServices,
  ...bodyServices,
  ...intimateServices,
  ...packageServices,
];

export function getServiceBySlug(slug: string): Service | undefined {
  return allServices.find((s) => s.slug === slug);
}

export function getServicesByCategory(categorySlug: string): Service[] {
  return allServices.filter((s) => s.categorySlug === categorySlug);
}

export function getCategoryBySlug(slug: string): ServiceCategory | undefined {
  return serviceCategories.find((c) => c.slug === slug);
}

export { serviceCategories };
```

Also re-export the individual arrays and slug constants for pages that need direct access.

After writing both files, run `npx tsc --noEmit` and then `npm run build` to confirm the full build passes.
  </action>
  <verify>
1. `npx tsc --noEmit` — zero errors
2. `npm run build` — succeeds with no TypeScript errors
3. Verify accessor logic: in a quick Node check or by reading the code, confirm:
   - `getServiceBySlug("brazilian-wax")` would match the Brazilian service
   - `getServicesByCategory("face")` would return 4 services
   - `getCategoryBySlug("packages")` would match the packages category
   - `allServices.length === 14`
  </verify>
  <done>categories.ts derives serviceSlugs from real arrays (not manual strings), index.ts exports 3 accessor functions + allServices array + re-exports, and `npm run build` passes cleanly.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero TypeScript errors
2. 14 services exist (4 + 5 + 3 + 2)
3. 4 categories exist with derived serviceSlugs
4. Every Service field is populated with real Honey & Bloom content (no placeholder text)
5. relatedServices on every service references slugs that exist in the catalog
6. First-Timer Package is accessible via `getServiceBySlug("first-timer-package")`
</verification>

<success_criteria>
- 14 services across face (4), body (5), intimate (3), packages (2) compile without errors
- Every service has unique preparation[], aftercare[], whatToExpect, faqs[], and metaTitle/metaDescription
- Accessor functions (getServiceBySlug, getServicesByCategory, getCategoryBySlug) are exported from index.ts
- ServiceCategory.serviceSlugs are derived from service arrays, not manually written
- `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-layer/03-01-SUMMARY.md`
</output>
