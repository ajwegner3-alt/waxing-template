---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - postcss.config.mjs
  - tsconfig.json
  - next.config.ts
  - src/app/globals.css
  - src/app/layout.tsx
  - src/content/client.config.ts
autonomous: true

must_haves:
  truths:
    - "Running `npm run build` exits 0 with no TypeScript errors"
    - "The blush cream background (#FAF3EF), honey gold (#D4A574), and sage green (#7A9E76) are available as Tailwind utility classes (bg-brand-light, bg-brand-primary, bg-brand-secondary)"
    - "Fraunces renders on headings and Plus Jakarta Sans renders on body text with zero layout shift (CLS 0)"
    - "Changing clientConfig.name updates the page title rendered by layout.tsx"
    - "The dual-change warning comment in globals.css maps every @theme token to its client.config.ts field"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Tailwind v4 @theme tokens for all brand colors and font references"
      contains: "@theme"
    - path: "src/app/layout.tsx"
      provides: "Font loading via next/font, html/body shell, clientConfig metadata wiring"
      exports: ["default RootLayout", "metadata"]
    - path: "src/content/client.config.ts"
      provides: "Single operator-editable config — all business info and brand colors"
      exports: ["clientConfig"]
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS variable names from next/font must match --font-* tokens in @theme"
      pattern: "--font-fraunces.*--font-plus-jakarta-sans"
    - from: "src/content/client.config.ts"
      to: "src/app/globals.css"
      via: "Dual-change: hex values in colors{} must match --color-brand-* in @theme"
      pattern: "colors\\."
---

<objective>
Scaffold a greenfield Next.js 16 project and lay the brand foundation: Tailwind v4 @theme design tokens, Fraunces + Plus Jakarta Sans fonts via next/font, and a single client.config.ts that owns all business data and color values.

Purpose: Every downstream component in Phases 2-10 imports from these three files. Getting tokens, fonts, and config right here prevents backtracking. No pages are built in this plan — only the building blocks.

Output: A running Next.js app at http://localhost:3000 with the correct blush cream background, honey gold and sage accent colors accessible as Tailwind utilities, and Fraunces headings loading without layout shift. client.config.ts is pre-populated with Honey & Bloom Wax Studio placeholder data.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\andre\OneDrive - Creighton University\Desktop\Claude-Code-Projects\website-creation\waxing-template\.planning\PROJECT.md
@C:\Users\andre\OneDrive - Creighton University\Desktop\Claude-Code-Projects\website-creation\waxing-template\.planning\ROADMAP.md
@C:\Users\andre\OneDrive - Creighton University\Desktop\Claude-Code-Projects\website-creation\waxing-template\.planning\phases\01-foundation\01-CONTEXT.md
@C:\Users\andre\OneDrive - Creighton University\Desktop\Claude-Code-Projects\website-creation\waxing-template\.planning\phases\01-foundation\01-RESEARCH.md

# Reference implementation (read and mirror the structural patterns — different identity)
@C:\Users\andre\OneDrive - Creighton University\Desktop\Claude-Code-Projects\website-creation\medspa-template\src\app\globals.css
@C:\Users\andre\OneDrive - Creighton University\Desktop\Claude-Code-Projects\website-creation\medspa-template\src\app\layout.tsx
@C:\Users\andre\OneDrive - Creighton University\Desktop\Claude-Code-Projects\website-creation\medspa-template\src\content\client.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project and configure Tailwind v4</name>
  <files>
    package.json
    postcss.config.mjs
    tsconfig.json
    next.config.ts
  </files>
  <action>
From the waxing-template directory (C:\Users\andre\OneDrive - Creighton University\Desktop\Claude-Code-Projects\website-creation\waxing-template), run:

```bash
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --yes
npm install motion
npm install tailwindcss @tailwindcss/postcss
```

After scaffold completes, update postcss.config.mjs to use the Tailwind v4 PostCSS plugin:
```js
// postcss.config.mjs
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
export default config;
```

Remove any generated tailwind.config.js or tailwind.config.ts — Tailwind v4 uses @theme in CSS, not a JS config file. The @import "tailwindcss" directive in globals.css replaces it.

Verify tsconfig.json has `"paths": { "@/*": ["./src/*"] }` in compilerOptions. This is typically set by create-next-app with --import-alias but confirm it.
  </action>
  <verify>
    Run `npm run build` from the project root. The build must exit 0. If it fails due to tailwind config conflicts, delete any .js tailwind config and re-run build.
  </verify>
  <done>
    `npm run build` exits 0. No tailwind.config.js exists. postcss.config.mjs uses @tailwindcss/postcss. tsconfig.json has "@/*" path alias.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write globals.css with @theme design tokens and base styles</name>
  <files>src/app/globals.css</files>
  <action>
Replace the generated globals.css entirely. The structure must mirror medspa-template/src/app/globals.css but with the waxing template's distinct identity.

Write the file with this exact content (values from RESEARCH.md color system section and CONTEXT.md decisions):

```css
/**
 * Honey & Bloom Wax Studio — Global Styles & Tailwind v4 Design Tokens
 *
 * DUAL-CHANGE WARNING:
 * Color values in the @theme block MUST match the colors object in:
 *   src/content/client.config.ts
 *
 * Token → config field mapping:
 *   --color-brand-primary        → colors.primary       (#D4A574)
 *   --color-brand-primary-dark   → colors.primaryDark   (#B8895A)
 *   --color-brand-primary-light  → colors.primaryLight  (#EDD4B2)
 *   --color-brand-secondary      → colors.secondary     (#7A9E76)
 *   --color-brand-secondary-light→ colors.secondaryLight(#96B592)
 *   --color-brand-dark           → colors.dark          (#333333)
 *   --color-brand-light          → colors.light         (#FAF3EF)
 *   --color-brand-gold           → colors.gold          (#D4A574)
 *   --color-brand-urgency        → colors.urgency       (#F8EAE1)
 *
 * Font tokens reference CSS variables injected by next/font/google in layout.tsx.
 * The variable names (--font-fraunces, --font-plus-jakarta-sans) MUST match
 * the `variable` option passed to each font in layout.tsx.
 */

@import "tailwindcss";

@theme {
  /* --- Brand Colors --- */
  --color-brand-primary: #D4A574;
  --color-brand-primary-dark: #B8895A;
  --color-brand-primary-light: #EDD4B2;
  --color-brand-secondary: #7A9E76;
  --color-brand-secondary-light: #96B592;
  --color-brand-dark: #333333;
  --color-brand-light: #FAF3EF;
  --color-brand-gold: #D4A574;
  --color-brand-urgency: #F8EAE1;

  /* --- Typography --- */
  /* These reference CSS custom properties injected by next/font/google. */
  /* Variable names must match the `variable` config in src/app/layout.tsx. */
  --font-heading: var(--font-fraunces);
  --font-body: var(--font-plus-jakarta-sans);
}

/* ---------------------------------------------------------------------------
   Base Styles
   --------------------------------------------------------------------------- */

body {
  font-family: var(--font-body), system-ui, sans-serif;
  color: #333333; /* brand-dark */
  background-color: #FAF3EF; /* brand-light — blush cream */
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

h1,
h2,
h3,
h4,
h5,
h6 {
  font-family: var(--font-heading), "Georgia", serif;
  /* color inherited from body — removed explicit override so Tailwind text-white works on dark sections */
}

/* ---------------------------------------------------------------------------
   Focus Styles — Accessibility
   --------------------------------------------------------------------------- */

:focus-visible {
  outline: 2px solid #D4A574; /* brand-primary */
  outline-offset: 2px;
  border-radius: 2px;
}

/* ---------------------------------------------------------------------------
   Selection
   --------------------------------------------------------------------------- */

::selection {
  background-color: #F8EAE1; /* brand-urgency — warm blush selection */
  color: #333333;
}

/* ---------------------------------------------------------------------------
   Smooth Scroll
   --------------------------------------------------------------------------- */

html {
  scroll-behavior: smooth;
}

/* ---------------------------------------------------------------------------
   Noise Texture Utility
   Used as a subtle background texture on the body and select sections.
   Opacity is intentionally low (5%) — the effect should register
   subconsciously, not consciously. See RESEARCH.md pitfall #5.
   --------------------------------------------------------------------------- */

.noise-texture {
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.05'/%3E%3C/svg%3E");
  background-repeat: repeat;
  background-size: 200px 200px;
}

/* ---------------------------------------------------------------------------
   .prose-custom — Blog & Rich Text Content
   Applied to markdown-rendered blog post bodies and long-form service
   descriptions. Uses brand colors and Fraunces for headings.
   --------------------------------------------------------------------------- */

.prose-custom {
  font-family: var(--font-body), system-ui, sans-serif;
  color: #333333;
  line-height: 1.75;
  max-width: 72ch;
}

.prose-custom h1,
.prose-custom h2,
.prose-custom h3,
.prose-custom h4 {
  font-family: var(--font-heading), "Georgia", serif;
  color: #333333;
  font-weight: 400;
  margin-top: 2em;
  margin-bottom: 0.5em;
  line-height: 1.25;
}

.prose-custom h1 { font-size: 2.25rem; }
.prose-custom h2 { font-size: 1.75rem; }
.prose-custom h3 { font-size: 1.375rem; }

.prose-custom p {
  margin-bottom: 1.25em;
}

.prose-custom a {
  color: #D4A574; /* brand-primary */
  text-decoration: underline;
  text-underline-offset: 2px;
  text-decoration-thickness: 1px;
}

.prose-custom a:hover {
  color: #B8895A; /* brand-primary-dark */
}

.prose-custom ul,
.prose-custom ol {
  margin-bottom: 1.25em;
  padding-left: 1.5em;
}

.prose-custom ul { list-style-type: disc; }
.prose-custom ol { list-style-type: decimal; }

.prose-custom li {
  margin-bottom: 0.375em;
}

.prose-custom blockquote {
  border-left: 3px solid #D4A574; /* brand-primary */
  padding-left: 1.25em;
  margin-left: 0;
  font-style: italic;
  color: #B8895A; /* brand-primary-dark */
}

.prose-custom strong {
  font-weight: 600;
  color: #333333;
}

.prose-custom hr {
  border: none;
  border-top: 1px solid #EDD4B2; /* brand-primary-light */
  margin: 2em 0;
}

.prose-custom img {
  border-radius: 0.75rem;
  max-width: 100%;
  height: auto;
  margin: 1.5em 0;
}
```

**Critical:** The `--font-fraunces` and `--font-plus-jakarta-sans` variable names in @theme must match exactly what Task 3 passes to `Fraunces({ variable: ... })` and `Plus_Jakarta_Sans({ variable: ... })` in layout.tsx.
  </action>
  <verify>
    After `npm run dev`, open http://localhost:3000 in a browser. The background should be a warm blush cream, not white. Run: `grep -r "bg-brand-primary\|text-brand-secondary\|bg-brand-light" src/` to confirm Tailwind utility classes are referenced in the codebase (they'll be used from Plan 02 onward, but the CSS variables should be defined).
  </verify>
  <done>
    globals.css has the @theme block with all 9 color tokens and 2 font tokens. Background is #FAF3EF (blush cream), not the default white. The DUAL-CHANGE WARNING comment maps every token to its client.config.ts field.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create layout.tsx with font loading and client.config.ts with Honey & Bloom data</name>
  <files>
    src/app/layout.tsx
    src/content/client.config.ts
    src/lib/types.ts
  </files>
  <action>
**Step 1: Create src/lib/types.ts** (minimal version — full waxing interfaces come in Plan 02)

Create the file with just the WaxingClientConfig interface so client.config.ts has a type to import:

```typescript
/**
 * Waxing Template — TypeScript Interfaces
 * Single source of truth for all content types.
 *
 * WaxingClientConfig is defined here for Plan 01.
 * All content interfaces (Service, Staff, FAQ, etc.) are added in Plan 02.
 *
 * DUAL-CHANGE WARNING: color values in WaxingClientConfig.colors must match
 * @theme tokens in globals.css.
 */

export interface WaxingClientConfig {
  // --- Identity ---
  name: string;
  tagline: string;
  phone: string;
  email: string;
  address: {
    street: string;
    city: string;
    state: string;
    zip: string;
  };

  // --- Trust Signals ---
  yearsInBusiness: number;
  reviewCount: number;
  reviewAverage: number;
  googleBusinessUrl: string;

  // --- Brand Colors ---
  /**
   * DUAL-CHANGE: These hex values MUST match @theme tokens in globals.css.
   * primary       → --color-brand-primary
   * primaryDark   → --color-brand-primary-dark
   * primaryLight  → --color-brand-primary-light
   * secondary     → --color-brand-secondary
   * secondaryLight→ --color-brand-secondary-light
   * dark          → --color-brand-dark
   * light         → --color-brand-light
   * gold          → --color-brand-gold
   * urgency       → --color-brand-urgency
   */
  colors: {
    primary: string;
    primaryDark: string;
    primaryLight: string;
    secondary: string;
    secondaryLight: string;
    dark: string;
    light: string;
    gold: string;
    urgency: string;
  };

  // --- Typography ---
  fonts: {
    heading: string;
    body: string;
  };

  // --- Logo ---
  logo: string;

  // --- Geography ---
  primaryCity: string;
  primaryState: string;
  serviceRadius: string;

  // --- Hours ---
  hours: {
    monday: string;
    tuesday: string;
    wednesday: string;
    thursday: string;
    friday: string;
    saturday: string;
    sunday: string;
  };

  // --- Booking ---
  /** External booking system URL — no Vagaro/Booksy hard-coding */
  bookingUrl: string;

  // --- Social Media ---
  instagramUrl: string;
  facebookUrl: string;
  tiktokUrl: string;

  // --- Analytics ---
  googleAnalyticsId: string;

  // --- Business Identity ---
  showPricing: boolean;
  priceRange: "$" | "$$" | "$$$" | "$$$$";
  foundingYear: number;
  siteUrl: string;

  // --- Waxing Specialties ---
  /** Primary wax formula description, e.g. "honey-based hard wax" */
  waxFormula: string;
  /** Key selling points for first-timer messaging */
  waxingSpecialties: string[];
}
```

**Step 2: Create src/content/client.config.ts**

Create the directory first (`mkdir -p src/content`), then write:

```typescript
/**
 * Client Configuration — Honey & Bloom Wax Studio (Placeholder)
 *
 * This file is the single operator-editable config for a waxing studio deployment.
 * Replace all placeholder values with the actual client's information.
 *
 * DUAL-CHANGE WARNING:
 * Color hex values below MUST stay in sync with the @theme block in:
 *   src/app/globals.css
 *
 * If you update a color here, update the corresponding --color-brand-* token
 * in globals.css. These are two intentionally separate files.
 *
 * Token → field mapping:
 *   --color-brand-primary        → colors.primary       (#D4A574)
 *   --color-brand-primary-dark   → colors.primaryDark   (#B8895A)
 *   --color-brand-primary-light  → colors.primaryLight  (#EDD4B2)
 *   --color-brand-secondary      → colors.secondary     (#7A9E76)
 *   --color-brand-secondary-light→ colors.secondaryLight(#96B592)
 *   --color-brand-dark           → colors.dark          (#333333)
 *   --color-brand-light          → colors.light         (#FAF3EF)
 *   --color-brand-gold           → colors.gold          (#D4A574)
 *   --color-brand-urgency        → colors.urgency       (#F8EAE1)
 */

import type { WaxingClientConfig } from "@/lib/types";

export const clientConfig: WaxingClientConfig = {
  // ---------------------------------------------------------------------------
  // Identity
  // ---------------------------------------------------------------------------
  name: "Honey & Bloom Wax Studio",
  tagline: "Waxing that actually feels good",
  phone: "(402) 555-0140",
  email: "hello@honeyandbloomomaha.com",
  address: {
    street: "4567 Leavenworth Street",
    city: "Omaha",
    state: "Nebraska",
    zip: "68105",
  },

  // ---------------------------------------------------------------------------
  // Trust Signals
  // ---------------------------------------------------------------------------
  yearsInBusiness: 6,
  reviewCount: 247,
  reviewAverage: 4.9,
  googleBusinessUrl: "https://www.google.com/maps/place/honey-bloom-wax-studio-omaha",

  // ---------------------------------------------------------------------------
  // Brand Colors
  // DUAL-CHANGE: These hex values must match @theme tokens in globals.css.
  // ---------------------------------------------------------------------------
  colors: {
    primary: "#D4A574",       // warm honey gold — primary CTA color
    primaryDark: "#B8895A",   // deeper gold for hover states
    primaryLight: "#EDD4B2",  // light gold for subtle backgrounds, ghost states
    secondary: "#7A9E76",     // muted sage green — complementary accent
    secondaryLight: "#96B592",// lighter sage for hover/inactive states
    dark: "#333333",          // charcoal body text (LOCKED by user decision)
    light: "#FAF3EF",         // blush cream — page background
    gold: "#D4A574",          // same as primary — gold IS the brand here
    urgency: "#F8EAE1",       // soft peach-blush for promo banners
  },

  // ---------------------------------------------------------------------------
  // Typography
  // ---------------------------------------------------------------------------
  fonts: {
    heading: "Fraunces",          // loaded via next/font/google in layout.tsx
    body: "Plus Jakarta Sans",    // loaded via next/font/google in layout.tsx
  },

  // ---------------------------------------------------------------------------
  // Logo & Imagery
  // ---------------------------------------------------------------------------
  logo: "/images/logo.svg", // replace with actual logo

  // ---------------------------------------------------------------------------
  // Geography
  // ---------------------------------------------------------------------------
  primaryCity: "Omaha",
  primaryState: "Nebraska",
  serviceRadius: "Serving the Greater Omaha Area",

  // ---------------------------------------------------------------------------
  // Hours
  // ---------------------------------------------------------------------------
  hours: {
    monday: "10:00 AM – 7:00 PM",
    tuesday: "10:00 AM – 7:00 PM",
    wednesday: "10:00 AM – 7:00 PM",
    thursday: "10:00 AM – 7:00 PM",
    friday: "10:00 AM – 6:00 PM",
    saturday: "9:00 AM – 5:00 PM",
    sunday: "Closed",
  },

  // ---------------------------------------------------------------------------
  // Online Booking
  // External booking system URL — swap to Vagaro/Booksy/etc. for real client
  // ---------------------------------------------------------------------------
  bookingUrl: "https://booking.honeyandbloomomaha.com",

  // ---------------------------------------------------------------------------
  // Social Media
  // ---------------------------------------------------------------------------
  instagramUrl: "https://instagram.com/honeyandbloomomaha",
  facebookUrl: "https://facebook.com/honeyandbloomomaha",
  tiktokUrl: "https://tiktok.com/@honeyandbloomomaha",

  // ---------------------------------------------------------------------------
  // Analytics
  // ---------------------------------------------------------------------------
  googleAnalyticsId: "", // add G-XXXXXXXXXX when ready

  // ---------------------------------------------------------------------------
  // Pricing & Business Identity
  // ---------------------------------------------------------------------------
  showPricing: true,
  priceRange: "$$",
  foundingYear: 2019,
  siteUrl: "https://honeyandbloomomaha.com",

  // ---------------------------------------------------------------------------
  // Waxing Specialties
  // ---------------------------------------------------------------------------
  waxFormula: "honey-based hard wax",
  waxingSpecialties: [
    "Sensitive skin formulas",
    "First-timer friendly",
    "Brazilian wax specialists",
    "Natural honey-based wax",
  ],
};
```

**Step 3: Update src/app/layout.tsx**

Replace the generated layout.tsx. The font variable names `--font-fraunces` and `--font-plus-jakarta-sans` MUST match the `@theme` tokens in globals.css:

```typescript
/**
 * Root Layout — Honey & Bloom Wax Studio Template
 *
 * Loads Fraunces (heading) and Plus Jakarta Sans (body) via next/font/google.
 * The variable names passed here MUST match the --font-* tokens in globals.css:
 *   --font-fraunces          → used by --font-heading in @theme
 *   --font-plus-jakarta-sans → used by --font-body in @theme
 *
 * Fraunces is loaded as a variable font (no explicit weight array) to access
 * the full wght axis range (100-900) and the SOFT/WONK axes.
 * See RESEARCH.md pitfall #3 for why this matters.
 */

import type { Metadata } from "next";
import { Fraunces, Plus_Jakarta_Sans } from "next/font/google";
import "./globals.css";
import { clientConfig } from "@/content/client.config";

const fraunces = Fraunces({
  variable: "--font-fraunces",
  subsets: ["latin"],
  // No weight array — load as variable font for full axis access
  // Axes: wght (100-900), SOFT (0-100), WONK (0-1), opsz (9-144)
  display: "swap",
});

const plusJakartaSans = Plus_Jakarta_Sans({
  variable: "--font-plus-jakarta-sans",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700"],
  display: "swap",
});

export const metadata: Metadata = {
  title: `${clientConfig.name} — ${clientConfig.tagline}`,
  description: `Professional waxing services in ${clientConfig.primaryCity}. Honey-based wax, first-timer friendly, ${clientConfig.reviewCount}+ five-star reviews. Book online today.`,
  metadataBase: new URL(clientConfig.siteUrl),
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" className={`${fraunces.variable} ${plusJakartaSans.variable}`}>
      <body className="antialiased noise-texture">
        <main>{children}</main>
      </body>
    </html>
  );
}
```

Note: Header, Footer, and SchemaScript are NOT added here yet — those belong to Phase 2 (Layout Shell) and Plan 02 respectively. This layout is the minimal shell for Phase 1.
  </action>
  <verify>
    Run `npm run build`. Must exit 0 with no TypeScript errors. Then run `npm run dev` and visit http://localhost:3000. The page title in the browser tab should read "Honey & Bloom Wax Studio — Waxing that actually feels good". Inspect the html element in DevTools — it should have class attributes containing "fraunces" and "plus-jakarta-sans" variable font CSS classes.
  </verify>
  <done>
    `npm run build` exits 0. src/lib/types.ts exports WaxingClientConfig. src/content/client.config.ts exports clientConfig typed as WaxingClientConfig. layout.tsx loads Fraunces (variable font, no weight array) and Plus Jakarta Sans with display swap. Browser tab shows "Honey & Bloom Wax Studio — Waxing that actually feels good". The html element has both font variable CSS class attributes.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. `npm run build` exits 0 — no TypeScript errors, no missing modules
2. `npm run dev` serves at http://localhost:3000 with blush cream (#FAF3EF) background
3. Browser DevTools Elements panel: html element has both `--font-fraunces` and `--font-plus-jakarta-sans` font CSS variable classes
4. Page title reads "Honey & Bloom Wax Studio — Waxing that actually feels good"
5. globals.css has the DUAL-CHANGE WARNING comment with the full token-to-field mapping table
6. client.config.ts imports `WaxingClientConfig` from `@/lib/types`
7. No tailwind.config.js exists in the project root
</verification>

<success_criteria>
- Tailwind v4 @theme tokens define 9 color utilities (bg-brand-primary, bg-brand-light, etc.) and 2 font tokens
- Fraunces renders on h1-h6 elements (confirm in DevTools computed styles)
- Plus Jakarta Sans renders on body text
- client.config.ts change to `name` field propagates through layout.tsx metadata
- `npm run build` exits 0
- No content from Phase 2 is created here (no Header, Footer, or layout components)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` following the summary template at @C:\Users\andre\.claude/get-shit-done/templates/summary.md
</output>
