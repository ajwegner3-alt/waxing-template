---
phase: 05-service-pages
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/services/PainLevelBadge.tsx
  - src/components/services/IngredientCallout.tsx
  - src/components/services/FAQAccordion.tsx
  - src/components/services/ServiceTestimonial.tsx
  - src/components/services/RelatedServices.tsx
  - src/components/services/index.ts
  - src/app/services/[slug]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visiting /services/brazilian-wax renders a complete service detail page with hero, prep, aftercare, FAQ, and related services"
    - "All 14 service slugs resolve to unique pages; unknown slugs 404"
    - "Pain level badge shows correct color (green for gentle, amber for moderate, rose for intense)"
    - "Sensitive-skin badge appears when service.sensitiveSkintSafe is true"
    - "Prep and aftercare are always visible (not hidden behind accordions)"
    - "Ingredient callout lists what the studio uses AND what it does not use"
    - "FAQ accordion expands/collapses with accessible details/summary elements"
    - "Related services section shows 2-3 ServiceCard links at bottom"
    - "Matching testimonial appears inline when one exists; gracefully absent when none"
  artifacts:
    - path: "src/app/services/[slug]/page.tsx"
      provides: "Dynamic service detail page with generateStaticParams and generateMetadata"
      exports: ["default", "generateStaticParams", "generateMetadata"]
    - path: "src/components/services/PainLevelBadge.tsx"
      provides: "Colored pain level pill badge"
      exports: ["PainLevelBadge"]
    - path: "src/components/services/IngredientCallout.tsx"
      provides: "Ingredient list + what-we-dont-use callout box"
      exports: ["IngredientCallout"]
    - path: "src/components/services/FAQAccordion.tsx"
      provides: "Accessible accordion using details/summary"
      exports: ["FAQAccordion"]
    - path: "src/components/services/ServiceTestimonial.tsx"
      provides: "Inline testimonial quote card"
      exports: ["ServiceTestimonial"]
    - path: "src/components/services/RelatedServices.tsx"
      provides: "You Might Also Like section with ServiceCard grid"
      exports: ["RelatedServices"]
  key_links:
    - from: "src/app/services/[slug]/page.tsx"
      to: "src/content/services/index.ts"
      via: "getServiceBySlug(slug)"
      pattern: "getServiceBySlug"
    - from: "src/app/services/[slug]/page.tsx"
      to: "src/content/testimonials.ts"
      via: "testimonial lookup by service.name"
      pattern: "testimonials.*find.*service"
    - from: "src/components/services/RelatedServices.tsx"
      to: "src/components/services/ServiceCard.tsx"
      via: "reuses ServiceCard from Plan 01"
      pattern: "ServiceCard"
    - from: "src/app/services/[slug]/page.tsx"
      to: "generateStaticParams"
      via: "allServices.map to slug objects"
      pattern: "generateStaticParams"
---

<objective>
Build the individual service detail page template at /services/[slug] with all 14 service pages statically generated. Each page follows an anxiety-first layout: hero with booking CTA, What to Expect, Prep, Aftercare, Ingredients callout, inline testimonial, FAQ accordion, Related Services, and bottom CTA.

Purpose: These are the highest-intent pages on the site. A visitor searching "Brazilian wax Omaha" lands directly here. Above-fold CTA catches ready-to-book visitors; anxiety-first content below converts hesitant first-timers.

Output: 5 new components (PainLevelBadge, IngredientCallout, FAQAccordion, ServiceTestimonial, RelatedServices) + /services/[slug] dynamic route generating all 14 service pages.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-service-pages/05-CONTEXT.md
@.planning/phases/05-service-pages/05-RESEARCH.md
@.planning/phases/05-service-pages/05-01-SUMMARY.md
@src/lib/types.ts
@src/content/services/index.ts
@src/content/services/face.ts
@src/content/testimonials.ts
@src/components/services/index.ts
@src/components/services/ServiceCard.tsx
@src/components/ui/index.ts
@src/components/ui/SectionWrapper.tsx
@src/components/ui/FadeUp.tsx
@src/components/ui/BookingLink.tsx
@src/components/ui/Badge.tsx
@src/components/ui/StarRating.tsx
@src/components/layout/Breadcrumbs.tsx
@src/lib/metadata.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Service detail components (PainLevelBadge, IngredientCallout, FAQAccordion, ServiceTestimonial, RelatedServices)</name>
  <files>
    src/components/services/PainLevelBadge.tsx
    src/components/services/IngredientCallout.tsx
    src/components/services/FAQAccordion.tsx
    src/components/services/ServiceTestimonial.tsx
    src/components/services/RelatedServices.tsx
    src/components/services/index.ts
  </files>
  <action>
**PainLevelBadge.tsx** (Server Component):
- Props: `painLevel: PainLevel`, `sensitiveSkintSafe: boolean`
- Renders a horizontal badge row with two items:
  1. Pain level pill: Map painLevel 1-2 to green (bg-brand-secondary/15 text-brand-secondary, label "Gentle"/"Very Gentle"), 3 to amber (bg-amber-100 text-amber-700, label "Moderate"), 4-5 to rose (bg-rose-100 text-rose-600, label "Intense"/"Very Intense")
  2. If sensitiveSkintSafe: render a second pill "Sensitive Skin Safe" in a soft green/trust style
- Each pill: `inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-sm font-medium`
- Include a small inline SVG icon per pill (leaf for gentle, flame for intense, shield-check for sensitive skin)

**IngredientCallout.tsx** (Server Component):
- Props: `ingredients: string[]`
- A callout box with: subtle bg (bg-brand-secondary/8 or bg-brand-light), rounded-2xl, p-6
- Headline: "What We Use" with a small honey/leaf inline SVG icon
- List the ingredients as pills or comma-separated styled text
- Below that: "What We Never Use" counter-list — hardcode: "No parabens", "No harsh chemicals", "No synthetic fragrances", "No rosin-based wax"
- The "never use" items should be visually distinct (muted text, strikethrough or crossed icon)
- If `ingredients.length === 0`, do not render the component at all

**FAQAccordion.tsx** (Server Component — uses native details/summary, NO "use client"):
- Props: `faqs: FAQ[]` (import FAQ from `@/lib/types`)
- Use native `<details>/<summary>` HTML elements — accessible by default, no JS state needed
- Each item: `details` with `className="group border border-brand-primary/15 rounded-xl overflow-hidden"`
- Summary: flex row with question text + chevron SVG that rotates via `group-open:rotate-180 transition-transform`
- Answer: `div` below summary with `px-5 pb-4 text-brand-dark/75 leading-relaxed`
- Remove default `<summary>` marker with `list-none` and `[&::-webkit-details-marker]:hidden`
- If `faqs.length === 0`, do not render

**ServiceTestimonial.tsx** (Server Component):
- Props: `testimonial: Testimonial | null`
- If null, render nothing (return null)
- Quote card: bg-brand-light rounded-2xl p-6, blockquote with large opening quote mark (decorative), testimonial.quote in italic, reviewer name + StarRating below
- Wrap in FadeUp

**RelatedServices.tsx** (Server Component):
- Props: `services: Service[]` (already resolved — page passes the looked-up Service objects, not slugs)
- Heading: H2 "You Might Also Like"
- Grid of ServiceCard components (reuse from Plan 01): `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
- Wrap in SectionWrapper bg="light", FadeUp
- If services.length === 0, do not render

**Update index.ts**: Add all 5 new component exports to the barrel.
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors. Verify all 5 new files exist with `ls src/components/services/`. Check barrel export includes all 8 components (3 from Plan 01 + 5 new).
  </verify>
  <done>All 5 components compile cleanly. PainLevelBadge maps all 5 pain levels to correct colors. FAQAccordion uses native details/summary (no "use client"). IngredientCallout conditionally renders. ServiceTestimonial handles null gracefully. RelatedServices reuses ServiceCard from Plan 01.</done>
</task>

<task type="auto">
  <name>Task 2: /services/[slug] dynamic route with generateStaticParams and full page composition</name>
  <files>
    src/app/services/[slug]/page.tsx
  </files>
  <action>
Create `src/app/services/[slug]/page.tsx`:

1. **generateStaticParams**: Map `allServices` to `{ slug: string }[]`. Export `const dynamicParams = false`.

2. **generateMetadata**: Async function, `await params` to get slug. Look up service via `getServiceBySlug(slug)`. If not found, return `{}`. Otherwise call `generatePageMetadata()` with `service.metaTitle`, `service.metaDescription`, path `/services/${service.slug}`.

3. **CRITICAL: params is a Promise** — both `generateMetadata` and the page component must `await params` before accessing slug. Type: `params: Promise<{ slug: string }>`.

4. **Page component** (async Server Component):
   ```
   const { slug } = await params;
   const service = getServiceBySlug(slug);
   if (!service) notFound();
   ```

5. **Testimonial lookup**: Import `testimonials` from `@/content/testimonials`. Find first where `t.service?.toLowerCase() === service.name.toLowerCase()`. Pass result (or null) to ServiceTestimonial.

6. **Related services lookup**: Map `service.relatedServices` through `getServiceBySlug`, filter Boolean, slice(0, 3).

7. **Section composition** (anxiety-first order per CONTEXT.md):

   a. **Breadcrumbs**: `items={[{ label: "Services", href: "/services" }, { label: service.name }]}`

   b. **Hero section**: SectionWrapper bg="light". Image placeholder div (aspect-[16/7], bg-gradient-to-br from-brand-primary-light via-brand-light to-brand-secondary/20, rounded-2xl). Below: H1 (`service.heroHeadline`), subtitle (`service.heroSubheadline`), price display (`service.priceDisplay` in large text), duration, PainLevelBadge row, and BookingLink (cta variant). Do NOT use negative margin — interior pages use standard spacing.

   c. **What to Expect**: SectionWrapper bg="white". H2, paragraph from `service.whatToExpect`. FadeUp.

   d. **Prep Instructions**: SectionWrapper bg="light". H2 "How to Prepare". Ordered list `<ol>` rendering `service.preparation[]` items as numbered steps. Always visible (NOT accordion). Style: numbered with brand-primary color on numbers.

   e. **Aftercare Tips**: SectionWrapper bg="white". H2 "Aftercare Tips". Ordered list from `service.aftercare[]`. Always visible. Different icon/color from prep to distinguish.

   f. **Ingredients Callout**: `<IngredientCallout ingredients={service.ingredients} />` — component handles empty array by not rendering.

   g. **Inline Testimonial**: `<ServiceTestimonial testimonial={matchedTestimonial} />` — component handles null by not rendering.

   h. **FAQ Accordion**: SectionWrapper. H2 "Frequently Asked Questions". `<FAQAccordion faqs={service.faqs} />` — component handles empty array.

   i. **Related Services**: `<RelatedServices services={relatedServices} />`

   j. **Bottom CTA**: SectionWrapper bg="primary". Warm message: "Ready to book your {service.name}?" + BookingLink (cta variant) + PhoneLink.

8. **Imports**: All from correct paths. `notFound` from `next/navigation`. Services components from `@/components/services`. UI components from `@/components/ui`. Breadcrumbs from `@/components/layout/Breadcrumbs`.
  </action>
  <verify>
Run `npm run build` — should generate all 14 static service pages with zero errors. Check build output lists `/services/brazilian-wax`, `/services/eyebrow-wax`, `/services/bikini-wax`, etc. Open localhost:3000/services/brazilian-wax in browser and confirm all sections render in order. Visit /services/nonexistent-slug and confirm 404.
  </verify>
  <done>All 14 service detail pages generate statically. Each page shows: hero with price + CTA, What to Expect, Prep list, Aftercare list, Ingredients callout (when present), Testimonial (when matched), FAQ accordion, Related Services, bottom CTA. Unknown slugs return 404. Metadata is unique per page.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds — generates /services (static) + 14 /services/[slug] pages
3. Browser: /services/brazilian-wax shows all sections in anxiety-first order
4. Browser: /services/eyebrow-wax shows "Gentle" green pain badge + "Sensitive Skin Safe" badge
5. Browser: /services/brazilian-wax shows inline testimonial; /services/back-wax does NOT (no matching testimonial)
6. FAQ accordion expands/collapses without JavaScript errors
7. Related Services shows 2-3 ServiceCard links at bottom of each detail page
8. /services/fake-slug returns 404
9. Each page has unique title and meta description
</verification>

<success_criteria>
- All 14 service slugs render unique pages with correct content
- dynamicParams = false — unknown slugs 404 at build time
- Pain badges: green (levels 1-2), amber (3), rose (4-5)
- Sensitive-skin badge appears only when sensitiveSkintSafe is true
- Prep and aftercare are always-visible ordered lists (NOT accordions)
- Ingredient callout conditionally renders
- Testimonial conditionally renders (5 services have matches, 9 do not)
- FAQ uses native details/summary (no "use client" needed)
- RelatedServices reuses ServiceCard component from Plan 01
- No "use client" except CategoryPills from Plan 01
- generateMetadata awaits params (Promise pattern for Next.js 16)
</success_criteria>

<output>
After completion, create `.planning/phases/05-service-pages/05-02-SUMMARY.md`
</output>
