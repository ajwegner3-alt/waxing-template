---
phase: 05-service-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/services/ServiceCard.tsx
  - src/components/services/CategorySection.tsx
  - src/components/services/CategoryPills.tsx
  - src/components/services/index.ts
  - src/app/services/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visitor sees all 14 services organized into 4 category sections on /services"
    - "Each service card shows name, price (with null fallback), and duration"
    - "Sticky category pills let visitor jump to any section without losing scroll position"
    - "Cards link to individual service detail pages at /services/[slug]"
  artifacts:
    - path: "src/components/services/ServiceCard.tsx"
      provides: "Compact service card — name, priceDisplay, duration, link"
      exports: ["ServiceCard"]
    - path: "src/components/services/CategorySection.tsx"
      provides: "Stacked category section with heading, description, and ServiceCard grid"
      exports: ["CategorySection"]
    - path: "src/components/services/CategoryPills.tsx"
      provides: "use client sticky pill bar with IntersectionObserver scroll tracking"
      exports: ["CategoryPills"]
    - path: "src/components/services/index.ts"
      provides: "Barrel export for all services components"
    - path: "src/app/services/page.tsx"
      provides: "Service menu page with metadata"
  key_links:
    - from: "src/app/services/page.tsx"
      to: "src/content/services/index.ts"
      via: "import { serviceCategories, allServices }"
      pattern: "serviceCategories|allServices"
    - from: "src/components/services/CategoryPills.tsx"
      to: "category section IDs"
      via: "smooth scroll to #category-{slug}"
      pattern: "category-.*scroll"
    - from: "src/components/services/ServiceCard.tsx"
      to: "/services/[slug]"
      via: "Next.js Link"
      pattern: "href.*services/"
---

<objective>
Build the /services menu page showing all 14 waxing services organized into 4 stacked category sections (Face, Body, Intimate, Packages) with sticky category navigation pills and compact service cards linking to detail pages.

Purpose: The menu page is the browsing entry point — visitors scan categories and prices before drilling into individual service pages. Transparent pricing builds trust; category organization reduces cognitive load.

Output: ServiceCard, CategorySection, CategoryPills components + /services route rendering all 14 services.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-service-pages/05-CONTEXT.md
@.planning/phases/05-service-pages/05-RESEARCH.md
@src/lib/types.ts
@src/content/services/index.ts
@src/content/services/categories.ts
@src/components/ui/index.ts
@src/components/ui/SectionWrapper.tsx
@src/components/ui/FadeUp.tsx
@src/components/ui/Badge.tsx
@src/components/layout/Breadcrumbs.tsx
@src/app/layout.tsx
@src/lib/metadata.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ServiceCard + CategorySection + CategoryPills components</name>
  <files>
    src/components/services/ServiceCard.tsx
    src/components/services/CategorySection.tsx
    src/components/services/CategoryPills.tsx
    src/components/services/index.ts
  </files>
  <action>
Create `src/components/services/` directory with these components:

**ServiceCard.tsx** (Server Component):
- Props: `service: Service` (import from `@/lib/types`)
- Renders: Next.js `Link` wrapping a card with service name (font-semibold), `service.priceDisplay` (never raw `service.price`), `service.duration`, and `service.shortDescription` truncated to 2 lines
- Card style: rounded-xl border border-brand-primary/10, p-5, hover:shadow-md transition, bg-white
- Link href: `/services/${service.slug}`
- Include a subtle arrow or "Learn More" text hint for clickability
- Wrap in `FadeUp` for entrance animation

**CategorySection.tsx** (Server Component):
- Props: `category: ServiceCategory`, `services: Service[]`
- Renders: `SectionWrapper` (alternate bg="white" and bg="light" per category — first/third white, second/fourth light)
- Section must have `id={`category-${category.slug}`}` and `className="scroll-mt-[4.5rem] lg:scroll-mt-24"` to clear the fixed header + pills
- Category heading (H2) using `category.name`, tagline as subtitle text
- Grid of `ServiceCard` components: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
- FadeUp on the heading, stagger delay on cards

**CategoryPills.tsx** ("use client"):
- Props: `categories: ServiceCategory[]`
- Sticky bar below header: `sticky top-16 lg:top-20 z-30 bg-white/95 backdrop-blur-sm border-b border-brand-primary/10`
- Horizontal scrollable row of pill buttons, one per category
- On click: `document.getElementById(`category-${slug}`)?.scrollIntoView({ behavior: "smooth" })`
- Active pill tracking via IntersectionObserver: observe each `#category-{slug}` section, update active state when section enters viewport (threshold 0.2, rootMargin "-80px 0px -60% 0px" to account for header)
- Active pill: `bg-brand-primary text-white`. Inactive: `bg-brand-light text-brand-dark hover:bg-brand-primary/10`
- Pills should be horizontally scrollable on mobile with `overflow-x-auto scrollbar-hide flex gap-2 px-4 py-3`
- Import from `motion/react` (NOT framer-motion)

**index.ts** (barrel export):
- Export all three components. This barrel will grow in Plan 02.
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors. Verify files exist with `ls src/components/services/`.
  </verify>
  <done>Three components export cleanly with correct types. ServiceCard uses priceDisplay (not raw price). CategoryPills is the only "use client" component. CategorySection has scroll-mt and anchor ID.</done>
</task>

<task type="auto">
  <name>Task 2: /services menu page with metadata</name>
  <files>
    src/app/services/page.tsx
  </files>
  <action>
Create `src/app/services/page.tsx` as a Server Component:

1. **Metadata**: Use `generatePageMetadata()` from `@/lib/metadata`:
   - title: "Waxing Services & Pricing in Omaha | Honey & Bloom Wax Studio"
   - description: "Browse our full menu of face, body, and intimate waxing services with transparent pricing. Gentle honey-based formula, first-timer friendly. Book your appointment today."
   - path: "/services"

2. **Page structure**:
   - `Breadcrumbs` with items: `[{ label: "Services", href: "/services" }]`
   - Page hero section: H1 "Our Waxing Services", subtitle paragraph about transparent pricing and gentle approach. Wrap in `SectionWrapper bg="light"` with `FadeUp`.
   - `CategoryPills` receiving `serviceCategories`
   - Map over `serviceCategories`, rendering a `CategorySection` for each with services filtered by `getServicesByCategory(category.slug)`

3. **Imports**:
   - `serviceCategories` and `getServicesByCategory` from `@/content/services`
   - `generatePageMetadata` from `@/lib/metadata`
   - `Breadcrumbs` from `@/components/layout/Breadcrumbs`
   - `SectionWrapper`, `FadeUp` from `@/components/ui`
   - `CategoryPills`, `CategorySection` from `@/components/services`

4. **Bottom CTA**: After all categories, add a `SectionWrapper bg="primary"` with a warm CTA: "Ready to feel smooth?" + BookingLink (cta variant). Import `BookingLink` from `@/components/ui`.
  </action>
  <verify>
Run `npm run build` — the /services route should build successfully with no errors. Open in browser at localhost:3000/services and confirm 4 category sections render with all 14 service cards.
  </verify>
  <done>/services page renders all 14 services across 4 stacked categories with sticky pills, proper metadata, breadcrumbs, and a bottom CTA. Each card links to /services/[slug].</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds — /services route generates as static page
3. Browser check: /services shows 4 category sections, 14 service cards total
4. Cards display name, priceDisplay, duration, short description
5. Category pills stick below header on scroll, clicking scrolls to correct section
6. Service cards link to /services/[slug] URLs
</verification>

<success_criteria>
- All 14 services visible on /services, organized into Face (4), Body (5), Intimate (3), Packages (2)
- Sticky category pills navigate between sections
- Each card links to its detail page URL
- Page has correct metadata (title, description, OG, canonical)
- No "use client" except CategoryPills
- ServiceCard component is reusable (will be used in Plan 02 RelatedServices)
</success_criteria>

<output>
After completion, create `.planning/phases/05-service-pages/05-01-SUMMARY.md`
</output>
