---
phase: 07-booking-flow
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/booking/ConfirmationStep.tsx
  - src/components/booking/index.ts
  - src/components/booking/BookingFlow.tsx
  - src/app/book/page.tsx
autonomous: true

must_haves:
  truths:
    - "User sees full appointment summary on step 4: services, total price, date, time, esthetician"
    - "Confirmation step shows service-specific prep reminders pulled from selected services data"
    - "Cancellation policy is displayed warmly on confirmation step"
    - "A warm redirect message frames the external booking handoff"
    - "Handoff button links to clientConfig.bookingUrl via BookingLink"
    - "/book page loads with a warm intro section and the 4-step booking flow"
  artifacts:
    - path: "src/components/booking/ConfirmationStep.tsx"
      provides: "Appointment summary, merged prep reminders, cancellation policy, warm handoff CTA"
      contains: "BookingLink"
    - path: "src/app/book/page.tsx"
      provides: "Server Component page with metadata, breadcrumbs, warm intro, BookingFlow"
      contains: "generatePageMetadata"
    - path: "src/components/booking/index.ts"
      provides: "Updated barrel with ConfirmationStep"
      contains: "ConfirmationStep"
  key_links:
    - from: "BookingFlow.tsx"
      to: "ConfirmationStep"
      via: "conditional render when state.step === 4"
      pattern: "step === 4"
    - from: "ConfirmationStep.tsx"
      to: "BookingLink"
      via: "external handoff CTA"
      pattern: "BookingLink"
    - from: "ConfirmationStep.tsx"
      to: "Service.preparation[]"
      via: "merged prep reminders from selected services"
      pattern: "preparation"
    - from: "page.tsx"
      to: "BookingFlow"
      via: "Server Component passing allServices and staff[0] as props"
      pattern: "BookingFlow.*services.*esthetician"
---

<objective>
Complete the booking flow with the ConfirmationStep (summary, prep, cancellation, warm handoff) and create the /book page that wires the Server Component intro with the BookingFlow client component.

Purpose: Finishes the 4-step booking UI. After this plan, /book is a fully navigable front-end booking flow that ends with a warm redirect to the external booking system.
Output: ConfirmationStep component, updated barrel/BookingFlow, and the /book page route.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-booking-flow/07-RESEARCH.md
@.planning/phases/07-booking-flow/07-CONTEXT.md
@.planning/phases/07-booking-flow/07-01-SUMMARY.md
@src/lib/types.ts
@src/lib/metadata.ts
@src/content/services/index.ts
@src/content/staff.ts
@src/content/client.config.ts
@src/components/booking/BookingFlow.tsx
@src/components/booking/index.ts
@src/components/ui/BookingLink.tsx
@src/components/ui/Badge.tsx
@src/components/ui/SectionWrapper.tsx
@src/components/ui/FadeUp.tsx
@src/components/layout/Breadcrumbs.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: ConfirmationStep + wire into BookingFlow + update barrel</name>
  <files>
    src/components/booking/ConfirmationStep.tsx
    src/components/booking/BookingFlow.tsx
    src/components/booking/index.ts
  </files>
  <action>
**ConfirmationStep.tsx** (no "use client" — child of client boundary):
- Props: { services: Service[], selectedSlugs: string[], esthetician: Staff, date: string, time: string }
- Compute selectedServices by filtering services against selectedSlugs
- Compute runningTotal: selectedServices.reduce((sum, s) => sum + (s.price ?? 0), 0) — guard nullable price
- Merge prep reminders: [...new Set(selectedServices.flatMap(s => s.preparation))] — deduplicate across multiple services
- Format the date string nicely for display (parse "2026-04-07" to "Monday, April 7, 2026" using static Date formatting — no library needed)

Layout sections (top to bottom):
1. **Heading**: "Your appointment summary" with warm subtext ("Here's what we have for you")
2. **Services list**: Each selected service with name and price. Total at bottom in bold. Use a clean card layout (bg-white rounded-2xl border p-6).
3. **Appointment details**: Date (formatted), time, esthetician name + title. Use icon-like indicators (inline SVG calendar icon, clock icon, person icon).
4. **Prep reminders**: Heading "How to prepare". Numbered list of merged prep items. Use brand-primary/15 background circles for numbers (matches Phase 5 pattern from STATE.md: "Prep uses brand-primary/15 circles").
5. **Cancellation policy**: Warm, non-legalistic tone. Static inline text: "We ask for at least 24 hours' notice if you need to cancel or reschedule. Late cancellations (under 24 hours) may incur a $15 fee. Life happens — just let us know as early as you can!" Use a subtle bg-brand-gold/10 or bg-brand-light card with a small info icon.
6. **Warm handoff section**: Heading like "Almost done!" with message: "Click below to confirm your appointment on our secure booking system. You'll be able to finalize the details there." Then render BookingLink variant="cta" with text "Confirm & Book". This links to clientConfig.bookingUrl.

**BookingFlow.tsx** — update:
- Import ConfirmationStep from "./ConfirmationStep"
- Replace the step 4 placeholder with the real ConfirmationStep render, passing: services, selectedSlugs: state.selectedServiceSlugs, esthetician, date: state.selectedDate!, time: state.selectedTime!

**index.ts** — update:
- Add export for ConfirmationStep
  </action>
  <verify>
Run `npx tsc --noEmit` — no TypeScript errors. Verify ConfirmationStep imports BookingLink. Verify price guard `?? 0` in total computation. Verify prep reminders use Set for deduplication. Verify BookingFlow step 4 renders ConfirmationStep (not placeholder). Verify barrel exports all 6 components.
  </verify>
  <done>
ConfirmationStep renders full appointment summary with services, total, date/time, esthetician, personalized prep reminders, cancellation policy, and warm BookingLink handoff CTA. BookingFlow step 4 is fully wired. Barrel exports complete.
  </done>
</task>

<task type="auto">
  <name>Task 2: /book page route with metadata, breadcrumbs, and warm intro</name>
  <files>
    src/app/book/page.tsx
  </files>
  <action>
**page.tsx** — Server Component (no "use client"):
- Import { Metadata } from "next" (type import)
- Import { generatePageMetadata } from "@/lib/metadata"
- Import { allServices } from "@/content/services"
- Import { staff } from "@/content/staff"
- Import { SectionWrapper, FadeUp } from "@/components/ui"
- Import { Breadcrumbs } from "@/components/layout"
- Import { BookingFlow } from "@/components/booking"

Export metadata using generatePageMetadata:
- title: "Book Your Appointment | Honey & Bloom Wax Studio Omaha"
- description: "Book a waxing appointment with Maya Chen at Honey & Bloom. Choose your services, date, and time in 2 minutes. Transparent pricing, gentle honey-based wax."
- path: "/book"

Export default BookPage function:
1. SectionWrapper bg="light" padding="sm" containing:
   - Breadcrumbs with items={[{ label: "Book", href: "/book" }]} and className="mb-4"
   - FadeUp wrapping:
     - h1: "Let's book your appointment" — font-heading text-3xl lg:text-4xl font-bold text-brand-dark mb-3
     - p: "Takes about 2 minutes. Pick your services, choose a time, and we'll take it from there." — text-brand-dark/70 text-lg leading-relaxed
     - max-w-xl container for the text
2. BookingFlow component receiving services={allServices} and esthetician={staff[0]}

The page is the thin composer pattern established in Phase 4: metadata + render order, no layout logic. Data is passed from Server Component to client component as serializable props.
  </action>
  <verify>
Run `npx tsc --noEmit` — no TypeScript errors. Run `npm run build` to verify the /book page statically generates (or at minimum compiles). Verify page.tsx has NO "use client" directive. Verify it imports from "@/components/booking" barrel. Verify metadata uses generatePageMetadata. Visit localhost:3000/book in dev server to confirm the page renders.
  </verify>
  <done>
/book page renders with warm intro section (breadcrumbs, heading, subtext) above the interactive 4-step BookingFlow. Page metadata is set. The full flow is navigable: select services -> see esthetician -> pick date/time -> review summary with prep and handoff CTA. TypeScript compiles and page builds without errors.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds — /book page generates
3. /book page has correct metadata (title, description)
4. ConfirmationStep shows all selected services with running total
5. Prep reminders are merged and deduplicated from selected services
6. Cancellation policy displayed with warm tone
7. BookingLink CTA on confirmation links to clientConfig.bookingUrl
8. Breadcrumbs render on /book page
9. No "use client" in page.tsx — Server Component pattern maintained
10. Full 4-step flow navigable: services -> esthetician -> date/time -> confirmation
</verification>

<success_criteria>
- /book page loads with warm intro and 4-step booking flow
- All 4 steps are fully interactive and navigable
- Confirmation step displays: services + total + date + time + esthetician + prep + cancellation + handoff
- BookingLink on confirmation redirects to clientConfig.bookingUrl
- Page builds and statically generates without errors
- Visual aesthetic is warm and guided, matching the rest of the site
</success_criteria>

<output>
After completion, create `.planning/phases/07-booking-flow/07-02-SUMMARY.md`
</output>
