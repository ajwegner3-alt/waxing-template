---
phase: 07-booking-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/booking/BookingFlow.tsx
  - src/components/booking/ProgressIndicator.tsx
  - src/components/booking/ServiceSelector.tsx
  - src/components/booking/EstheticianStep.tsx
  - src/components/booking/DateTimePicker.tsx
  - src/components/booking/index.ts
autonomous: true

must_haves:
  truths:
    - "User can select multiple services and see a running price total update"
    - "User sees Maya Chen auto-selected on esthetician step"
    - "User can pick a date from a calendar grid and choose a time slot"
    - "Step progress indicator shows current step with numbered circles"
    - "Back button returns to previous step; Continue disabled when step incomplete"
  artifacts:
    - path: "src/components/booking/BookingFlow.tsx"
      provides: "Client component step state machine with useReducer + AnimatePresence transitions"
      contains: "use client"
    - path: "src/components/booking/ProgressIndicator.tsx"
      provides: "4-step numbered circle + line progress bar"
    - path: "src/components/booking/ServiceSelector.tsx"
      provides: "Multi-select service cards grouped by category with running total"
      contains: "price ?? 0"
    - path: "src/components/booking/EstheticianStep.tsx"
      provides: "Auto-selected Maya Chen profile card"
    - path: "src/components/booking/DateTimePicker.tsx"
      provides: "Monthly calendar grid + time slot button grid with static mock data"
    - path: "src/components/booking/index.ts"
      provides: "Barrel export for all booking components"
  key_links:
    - from: "BookingFlow.tsx"
      to: "ServiceSelector, EstheticianStep, DateTimePicker"
      via: "conditional render based on state.step"
      pattern: "state\\.step === [123]"
    - from: "ServiceSelector.tsx"
      to: "BookingFlow dispatch"
      via: "onToggle callback dispatching TOGGLE_SERVICE"
      pattern: "TOGGLE_SERVICE"
    - from: "DateTimePicker.tsx"
      to: "BookingFlow dispatch"
      via: "onSelectDate/onSelectTime callbacks"
      pattern: "SELECT_DATE|SELECT_TIME"
---

<objective>
Build the interactive booking flow core: the BookingFlow client component with useReducer state machine, step transitions via AnimatePresence, progress indicator, and the first three step sub-components (ServiceSelector, EstheticianStep, DateTimePicker).

Purpose: Creates the interactive booking UI that lets users select services, see an esthetician, and pick a date/time. Plan 02 adds the confirmation step and /book page.
Output: 6 files in src/components/booking/ — the full interactive flow minus the confirmation step.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-booking-flow/07-RESEARCH.md
@.planning/phases/07-booking-flow/07-CONTEXT.md
@src/lib/types.ts
@src/content/services/index.ts
@src/content/services/categories.ts
@src/content/staff.ts
@src/components/ui/Button.tsx
@src/components/ui/BookingLink.tsx
@src/components/layout/MobileNav.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: BookingFlow shell + ProgressIndicator + ServiceSelector</name>
  <files>
    src/components/booking/BookingFlow.tsx
    src/components/booking/ProgressIndicator.tsx
    src/components/booking/ServiceSelector.tsx
  </files>
  <action>
Create three files:

**BookingFlow.tsx** ("use client"):
- Import { useReducer } from "react", { AnimatePresence, motion } from "motion/react"
- Import type { Service, Staff } from "@/lib/types"
- Define BookingState: { step: 1|2|3|4, selectedServiceSlugs: string[], selectedEstheticianSlug: string, selectedDate: string|null, selectedTime: string|null }
- Define BookingAction union: NEXT_STEP, PREV_STEP, TOGGLE_SERVICE, SELECT_DATE, SELECT_TIME
- bookingReducer function handling all 5 actions. NEXT_STEP clamps at 4, PREV_STEP clamps at 1. SELECT_DATE resets selectedTime to null.
- INITIAL_STATE with step: 1, selectedEstheticianSlug: "maya-chen", selectedServiceSlugs: [], selectedDate: null, selectedTime: null
- Props: { services: Service[], esthetician: Staff }
- Compute canContinue: step 1 requires selectedServiceSlugs.length > 0; step 2 always true (auto-selected); step 3 requires selectedDate AND selectedTime both non-null; step 4 always true
- Render: ProgressIndicator, then AnimatePresence mode="wait" with motion.div keyed by `step-${state.step}`. Transitions: initial={{ opacity: 0, x: 20 }}, animate={{ opacity: 1, x: 0 }}, exit={{ opacity: 0, x: -20 }}, transition={{ duration: 0.25, ease: [0.22, 1, 0.36, 1] }}
- Conditionally render ServiceSelector (step 1), EstheticianStep (step 2), DateTimePicker (step 3), or a placeholder div for step 4 (ConfirmationStep added in Plan 02)
- Step navigation below AnimatePresence: Back button (text link style, only visible when step > 1) and Continue button (brand-primary, disabled when !canContinue). Hide nav entirely on step 4. Button text: "Review Booking" on step 3, "Continue" otherwise.
- Wrap in section with className "max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-8 pb-32" (pb-32 accounts for BookingBar)

**ProgressIndicator.tsx** (no "use client" — child of client boundary):
- Props: { currentStep: number, totalSteps?: number }
- STEP_LABELS: ["Services", "Esthetician", "Date & Time", "Confirm"]
- Render numbered circles (w-8 h-8 rounded-full) connected by lines (h-0.5 flex-1). Completed steps: bg-brand-primary border-brand-primary text-white with checkmark. Current: bg-white border-brand-primary text-brand-primary. Future: bg-white border-brand-dark/20 text-brand-dark/40.
- Labels below circles, hidden on mobile (hidden sm:block). Current label in text-brand-primary, others text-brand-dark/40.
- Use role="list" and role="listitem" for accessibility. aria-current="step" on current.
- Connector lines: completed = bg-brand-primary, incomplete = bg-brand-dark/15. Transition-colors for smooth step change.

**ServiceSelector.tsx** (no "use client" — child of client boundary):
- Props: { services: Service[], selectedSlugs: string[], onToggle: (slug: string) => void }
- Import { serviceCategories } from "@/content/services" for category grouping
- Group services by category using serviceCategories. For each category, render a section with category name as h3 heading.
- Each service rendered as a button (not checkbox) with aria-pressed={isSelected}. Card layout: name (font-semibold), shortDescription (text-sm, text-brand-dark/60), duration (text-xs). Right side: price display (use service.price !== null ? `$${service.price}` : service.priceDisplay), selection indicator circle (filled green with checkmark SVG when selected, empty circle when not).
- Selected card: border-brand-primary bg-brand-primary/5 shadow-sm. Unselected: border-brand-dark/10 bg-white hover:border-brand-primary/40.
- Running total footer (sticky at bottom of scroll area or fixed below cards): show count of selected services and total price. Use `services.filter(s => selectedSlugs.includes(s.slug)).reduce((sum, s) => sum + (s.price ?? 0), 0)` for total. Guard nullable price with ?? 0.
- Running total only visible when selectedSlugs.length > 0. Background: bg-brand-light rounded-xl p-4.
  </action>
  <verify>
Run `npx tsc --noEmit` — no TypeScript errors. Check that BookingFlow.tsx has "use client" directive. Check that ServiceSelector uses `?? 0` for price guard. Check that AnimatePresence uses mode="wait". Check imports are from "motion/react" not "framer-motion".
  </verify>
  <done>
BookingFlow renders step 1 (ServiceSelector) with working multi-select, running total, category grouping, and progress indicator showing step 1 active. Continue button disabled when no services selected. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: EstheticianStep + DateTimePicker + barrel export</name>
  <files>
    src/components/booking/EstheticianStep.tsx
    src/components/booking/DateTimePicker.tsx
    src/components/booking/index.ts
  </files>
  <action>
Create three files:

**EstheticianStep.tsx** (no "use client" — child of client boundary):
- Props: { esthetician: Staff }
- Render a profile card for Maya Chen: name (text-2xl font-heading), title, headshot placeholder (styled div with initials, same pattern as Phase 6 EstheticianProfile — bg-brand-primary/10 rounded-full w-24 h-24 centered initials), specialties as inline tags/badges, yearsExperience, certifications list, and a brief excerpt from bio (bio[0] sliced or first 150 chars).
- Show a "Selected" badge/indicator (green checkmark with "Your esthetician" label) since this step auto-selects.
- Warm copy above: "You'll be in great hands" or similar.
- Card: bg-white rounded-2xl border border-brand-dark/10 p-6 shadow-sm. Centered layout.
- acceptingNewClients badge if true.

**DateTimePicker.tsx** (no "use client" — child of client boundary):
- Props: { selectedDate: string | null, selectedTime: string | null, onSelectDate: (date: string) => void, onSelectTime: (time: string) => void }
- MOCK_AVAILABLE_DATES: array of "YYYY-MM-DD" strings for April 2026, roughly Mon-Thu available (see RESEARCH.md for exact dates: 2026-04-07 through 2026-04-29, about 14 dates).
- MOCK_TIME_SLOTS: 30-minute intervals from 9:00 AM to 4:30 PM (16 slots).
- buildCalendarGrid(year, month) function: uses native Date to compute first day offset, days in month, chunks into weeks of 7. Returns (string | null)[][] where null = empty cell, string = "YYYY-MM-DD".
- Render month header: "April 2026" with no prev/next (static demo). Day-of-week labels (S M T W T F S).
- Calendar grid: 7-column grid. Each date cell is a button. Available dates (in MOCK_AVAILABLE_DATES) get interactive styles; unavailable dates are grayed out and disabled. Selected date: bg-brand-primary text-white. Available: bg-white hover:bg-brand-primary/10 text-brand-dark. Unavailable: text-brand-dark/20 cursor-not-allowed.
- Time slot section: only visible when selectedDate is not null. Heading "Available times" with the selected date formatted nicely (e.g., "Monday, April 7"). Grid of time slot buttons (grid-cols-4 on desktop, grid-cols-3 on mobile). Selected: bg-brand-primary text-white. Unselected: bg-white border border-brand-dark/10 hover:border-brand-primary.
- Do NOT use new Date() in the render path for "today" comparisons — all dates are static mock data. No hydration risk.

**index.ts** (barrel export):
- Export BookingFlow from "./BookingFlow"
- Export ProgressIndicator from "./ProgressIndicator"
- Export ServiceSelector from "./ServiceSelector"
- Export EstheticianStep from "./EstheticianStep"
- Export DateTimePicker from "./DateTimePicker"
- (ConfirmationStep will be added in Plan 02)
  </action>
  <verify>
Run `npx tsc --noEmit` — no TypeScript errors. Verify barrel export includes all 5 components. Verify MOCK_AVAILABLE_DATES uses "2026-04-" prefix. Verify DateTimePicker does not import any date library. Verify no "framer-motion" imports anywhere in src/components/booking/.
  </verify>
  <done>
All 5 booking components exist and compile. BookingFlow can render steps 1-3 fully interactive: service multi-select with category grouping and running total, auto-selected esthetician profile card, and calendar grid with time slot selection. Step 4 is a placeholder pending Plan 02. Barrel export ready for page.tsx import.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. All 6 files exist in src/components/booking/
3. BookingFlow.tsx has "use client" as first line
4. No imports from "framer-motion" — all motion imports from "motion/react"
5. Service.price guarded with ?? 0 in ServiceSelector running total
6. AnimatePresence uses mode="wait"
7. MOCK_AVAILABLE_DATES are static April 2026 strings
8. ProgressIndicator has aria-current="step" on active step
</verification>

<success_criteria>
- TypeScript compiles with zero errors
- BookingFlow renders a 4-step state machine (step 4 placeholder is fine)
- ServiceSelector groups by category, supports multi-select, shows running total
- EstheticianStep shows Maya Chen profile auto-selected
- DateTimePicker renders April 2026 calendar grid and time slots
- ProgressIndicator reflects current step visually
- Back/Continue navigation works with proper disable logic
</success_criteria>

<output>
After completion, create `.planning/phases/07-booking-flow/07-01-SUMMARY.md`
</output>
