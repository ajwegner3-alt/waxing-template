---
phase: 09-blog-and-service-areas
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/lib/schema.ts
  - src/app/sitemap.ts
  - src/app/service-areas/page.tsx
  - src/app/service-areas/[slug]/page.tsx
  - src/components/service-areas/ServiceAreaHero.tsx
  - src/components/service-areas/LocalContextSection.tsx
  - src/components/service-areas/ServiceHighlights.tsx
  - src/components/service-areas/LocalHighlightCard.tsx
  - src/components/service-areas/CoverageArea.tsx
  - src/components/service-areas/index.ts
autonomous: true

must_haves:
  truths:
    - "Service area pages exist for 6 Omaha neighborhoods with genuinely localized content (not city-name swaps)"
    - "Each service area page shows hero, local context, popular services, local highlight, nearby areas with cross-links, and booking CTA"
    - "/service-areas index page lists all 6 neighborhoods as cards linking to detail pages"
    - "All 9 new routes (1 blog index, 3 blog posts, 1 service areas index, 6 neighborhood pages) are included in /sitemap.xml output"
    - "BlogPosting and ServiceArea schema generators exist in schema.ts with full implementations replacing the stubs from Plan 01"
  artifacts:
    - path: "src/app/service-areas/[slug]/page.tsx"
      provides: "Dynamic route for 6 neighborhood pages"
      exports: ["default", "generateStaticParams", "generateMetadata"]
    - path: "src/app/service-areas/page.tsx"
      provides: "Service areas index listing all neighborhoods"
      exports: ["default", "metadata"]
    - path: "src/lib/schema.ts"
      provides: "Full generateBlogPostSchema + generateServiceAreaSchema replacing Plan 01 stubs"
      contains: "generateBlogPostSchema"
    - path: "src/app/sitemap.ts"
      provides: "Updated sitemap with blog + service area routes"
      contains: "blogRoutes"
    - path: "src/components/service-areas/index.ts"
      provides: "Barrel export for service area components"
  key_links:
    - from: "src/app/service-areas/[slug]/page.tsx"
      to: "src/content/service-areas.ts"
      via: "serviceAreas.find() by slug"
      pattern: "serviceAreas\\.find"
    - from: "src/app/service-areas/[slug]/page.tsx"
      to: "src/lib/schema.ts"
      via: "generateServiceAreaSchema()"
      pattern: "generateServiceAreaSchema"
    - from: "src/app/sitemap.ts"
      to: "src/lib/blog.ts"
      via: "getAllPosts() for blog routes"
      pattern: "getAllPosts"
    - from: "src/app/sitemap.ts"
      to: "src/content/service-areas.ts"
      via: "serviceAreas for area routes"
      pattern: "serviceAreas"
---

<objective>
Build service area pages for 6 Omaha neighborhoods, replace stub schema generators with full implementations for both blog posts and service areas, and update the sitemap to include all new routes.

Purpose: Service area pages target "[city] waxing" local SEO queries. Each neighborhood page has genuinely distinct content (not city-name swaps) pulled from the already-populated service-areas.ts data. The sitemap update ensures all new routes are discoverable by search engines.

Output: 6 working /service-areas/[slug] pages, a /service-areas index page, full schema generators replacing Plan 01 stubs, and an updated sitemap covering all blog + service area routes.
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-blog-and-service-areas/09-RESEARCH.md
@.planning/phases/09-blog-and-service-areas/09-01-SUMMARY.md

@src/content/service-areas.ts (6 ServiceArea objects with full data)
@src/lib/types.ts (ServiceArea interface, BlogPost interface, WaxingClientConfig)
@src/lib/schema.ts (has stub generateBlogPostSchema + generateServiceAreaSchema from Plan 01 — replace stubs with full implementations)
@src/app/sitemap.ts (current sitemap — ADD blog + service area routes)
@src/app/services/[slug]/page.tsx (reference pattern for dynamic route)
@src/content/services/index.ts (allServices for service highlights on area pages)
@src/content/testimonials.ts (global testimonials — filter by location for area pages)
@src/components/ui/index.ts (SectionWrapper, FadeUp, BookingLink, PhoneLink, SchemaScript)
@src/lib/blog.ts (getAllPosts — needed for sitemap update)
@src/content/client.config.ts (for schema generators)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Service area components, dynamic route, index page</name>
  <files>
    src/components/service-areas/ServiceAreaHero.tsx
    src/components/service-areas/LocalContextSection.tsx
    src/components/service-areas/ServiceHighlights.tsx
    src/components/service-areas/LocalHighlightCard.tsx
    src/components/service-areas/CoverageArea.tsx
    src/components/service-areas/index.ts
    src/app/service-areas/[slug]/page.tsx
    src/app/service-areas/page.tsx
  </files>
  <action>
    **Create service area components in `src/components/service-areas/`:**

    All components are Server Components (no "use client"). Follow existing component patterns (SectionWrapper bg variants, FadeUp for entrance animation, font-heading for headings, brand color tokens).

    1. **ServiceAreaHero.tsx** — Accepts ServiceArea prop.
       - bg="primary" SectionWrapper (matches service page hero pattern)
       - H1: area.heroHeadline
       - Subheadline: area.heroSubheadline
       - BookingLink CTA + PhoneLink
       - Negative margin pattern for edge-to-edge fill (same as HomepageHero: -mt-16 lg:-mt-20, pt-32 lg:pt-40)

    2. **LocalContextSection.tsx** — Accepts ServiceArea prop.
       - bg="cream" SectionWrapper
       - Display: population, neighborhoods list (as inline badges/pills), localContext paragraph, weatherContext as a callout/aside
       - Use FadeUp for entrance

    3. **ServiceHighlights.tsx** — Accepts ServiceArea prop + services array.
       - bg="white" SectionWrapper
       - H2: "Popular Waxing Services in {area.city}"
       - Show 3-4 service cards (select services relevant to the area persona — but since we don't have persona-to-service mapping, show a mix: 1 intimate, 1 face, 1 body service)
       - Each card: service name, shortDescription, price, link to /services/[slug]
       - Use the existing service data from content/services

    4. **LocalHighlightCard.tsx** — Accepts ServiceArea.localHighlight prop.
       - bg="blush" SectionWrapper
       - H2: localHighlight.title
       - Description paragraph
       - Neighborhood name as a subtle label

    5. **CoverageArea.tsx** — Accepts ServiceArea prop.
       - bg="white" SectionWrapper
       - H2: "Also Serving Nearby Areas"
       - nearbyAreas rendered as links or plain text:
         - For each nearbyArea name, slugify it (toLowerCase().replace(/\s+/g, "-")) and check if a matching slug exists in the serviceAreas array (import serviceAreas from @/content/service-areas)
         - If a matching service area page exists: render as a Link to /service-areas/[matched-slug]
         - If NO matching service area page exists: render as plain text (not a link) — this prevents broken links for areas like "Downtown Omaha" or "Benson" that have no dedicated page
       - Internal cross-linking between neighborhood pages (only for areas that have pages)

    6. **Barrel export `index.ts`** — export all 5 components.

    **Create `/service-areas/[slug]/page.tsx` — neighborhood detail page:**
    - Server Component
    - `generateStaticParams()` returns serviceAreas.map(a => ({ slug: a.slug }))
    - `dynamicParams = false`
    - `generateMetadata()` — title: "Waxing in {area.city} | Honey & Bloom", description: first 155 chars of area.heroSubheadline or localContext
    - `params` is `Promise<{ slug: string }>` — must await
    - Find area by slug, notFound() if not found
    - Layout order:
      1. Breadcrumbs: Home / Service Areas / {area.city}
      2. ServiceAreaHero
      3. LocalContextSection
      4. ServiceHighlights (pass 3-4 services from allServices)
      5. LocalHighlightCard
      6. Testimonials section — filter global testimonials where testimonial text or name loosely matches area.city. If no matches, omit the section entirely (no empty states). Use the existing testimonials data from content/testimonials.ts.
      7. CoverageArea
      8. Bottom CTA — SectionWrapper bg="primary" with "Serving {area.city} and surrounding areas" heading, BookingLink + PhoneLink
    - Add ServiceArea JSON-LD via SchemaScript — import `generateServiceAreaSchema` from `@/lib/schema` (stub exists from Plan 01, will be replaced with full implementation in Task 2)

    **Create `/service-areas/page.tsx` — index page:**
    - Server Component
    - Export metadata: title "Waxing Service Areas in Omaha | Honey & Bloom"
    - Breadcrumbs: Home / Service Areas
    - H1: "Omaha Area Waxing Services"
    - Subheadline about serving neighborhoods across the metro
    - Card grid (1 col mobile, 2 col md, 3 col lg):
      - Each card: area.city as heading, area.heroSubheadline as excerpt, population, Link to /service-areas/[slug]
      - Hover state matching blog card pattern
    - Use SectionWrapper and FadeUp
  </action>
  <verify>
    Run `npm run build` — all service area pages and index build successfully. No type errors.
    Run `npx tsc --noEmit` — no type errors (generateServiceAreaSchema stub from Plan 01 resolves the import).
    Visually inspect the component structure: 5 components + barrel + 2 pages.
  </verify>
  <done>
    6 service area detail pages build at /service-areas/[slug] with hero, local context, service highlights, local highlight, coverage area, and CTA. Index page at /service-areas lists all 6 neighborhoods as cards. All content is genuinely localized from the service-areas.ts data. CoverageArea only links to neighborhoods that have actual pages.
  </done>
</task>

<task type="auto">
  <name>Task 2: Schema generators (blog + service area) full implementation and sitemap update</name>
  <files>
    src/lib/schema.ts
    src/app/sitemap.ts
  </files>
  <action>
    **Replace stubs in `src/lib/schema.ts` with full schema generators:**

    Plan 01 added stub versions of these two functions. Replace the stub implementations (look for the `// TODO: Plan 02 replaces with full implementation` comments) with full versions. Keep the same function signatures.

    1. `generateBlogPostSchema(post: BlogPost, config: WaxingClientConfig)` — returns BlogPosting JSON-LD:
       ```
       @type: "BlogPosting"
       headline: post.title
       description: post.description
       author: { @type: "Person", name: post.author }
       publisher: { @type: ["LocalBusiness", "BeautyBusiness"], name: config.name, url: config.siteUrl }
       datePublished: post.date
       dateModified: post.date
       image: config.siteUrl + post.image
       url: config.siteUrl + "/blog/" + post.slug
       mainEntityOfPage: { @type: "WebPage", @id: url }
       ```

    2. `generateServiceAreaSchema(area: ServiceArea, config: WaxingClientConfig)` — returns LocalBusiness with areaServed:
       ```
       @type: ["LocalBusiness", "BeautyBusiness"]
       name: config.name
       url: config.siteUrl
       telephone: config.phone
       areaServed: { @type: "City", name: area.schema.areaServed, containedInPlace: { @type: "AdministrativeArea", name: area.county } }
       address: full PostalAddress from config
       ```

    The imports for BlogPost and ServiceArea should already exist from the stubs. Verify they're present, add if not.

    **Update `src/app/sitemap.ts` — add blog and service area routes:**

    - Import `getAllPosts` from `@/lib/blog` (NOTE: sitemap.ts must become async since getAllPosts is async)
    - Import `serviceAreas` from `@/content/service-areas`
    - Change `export default function sitemap()` to `export default async function sitemap()`
    - Add after serviceRoutes:
      ```
      const allPosts = await getAllPosts()

      const blogRoutes = allPosts.map(post => ({
        url: `${base}/blog/${post.slug}`,
        lastModified: new Date(post.date),
        changeFrequency: "monthly" as const,
        priority: 0.7,
      }))

      const serviceAreaRoutes = serviceAreas.map(area => ({
        url: `${base}/service-areas/${area.slug}`,
        lastModified: now,
        changeFrequency: "monthly" as const,
        priority: 0.8,
      }))
      ```
    - Add `/blog` and `/service-areas` to staticRoutes array:
      ```
      { url: `${base}/blog`, lastModified: now, changeFrequency: "weekly", priority: 0.7 },
      { url: `${base}/service-areas`, lastModified: now, changeFrequency: "monthly", priority: 0.7 },
      ```
    - Return: `[...staticRoutes, ...serviceRoutes, ...blogRoutes, ...serviceAreaRoutes]`
  </action>
  <verify>
    Run `npm run build` — all pages build successfully. Check build output for:
    - /blog and 3 /blog/[slug] routes
    - /service-areas and 6 /service-areas/[slug] routes
    - /sitemap.xml generated

    Run `npx tsc --noEmit` — no type errors.

    Verify sitemap route count increased from ~20 to ~30 (6 static + 14 services + 1 blog index + 3 blog posts + 1 service-areas index + 6 service areas = 31).
  </verify>
  <done>
    schema.ts exports full generateBlogPostSchema and generateServiceAreaSchema (replacing stubs). sitemap.ts includes blog routes, service area routes, and index pages. Full build succeeds with all ~31 routes.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds — all service area and blog routes in build output
2. /service-areas index shows 6 neighborhood cards
3. Each /service-areas/[slug] page shows genuinely distinct content (different heroHeadline, localContext, localHighlight, weatherContext)
4. Service area pages include ServiceArea JSON-LD schema
5. Blog post pages include BlogPosting JSON-LD schema
6. /sitemap.xml includes all blog and service area URLs (verify ~31 total entries)
7. Cross-linking works: CoverageArea links only to neighborhood pages that exist, unmatched nearby areas render as plain text
</verification>

<success_criteria>
- 6 service area pages render at /service-areas/midtown-omaha, /service-areas/west-omaha, etc. with distinct localized content
- /service-areas index page lists all 6 neighborhoods with links
- generateBlogPostSchema and generateServiceAreaSchema in schema.ts have full implementations (not stubs)
- sitemap.xml contains ~31 URLs covering all static, service, blog, and service area routes
- `npm run build` produces static pages for all routes with no errors
- CoverageArea nearbyAreas only link to areas with matching service area pages
</success_criteria>

<output>
After completion, create `.planning/phases/09-blog-and-service-areas/09-02-SUMMARY.md`
</output>
